# Punkt: 4.13 EO: snapshot, presentation-model, canonical output og invarianter

**Dato:** 2026-05-31
**Filer gennemgået:**
- `src/domain/erstatningsopgoerelse/snapshot/eoSnapshot.ts` (461 l. — autoritativ entry `computeEoSnapshot`)
- `src/domain/erstatningsopgoerelse/snapshot/eoCanonicalOutput.ts` (198 l.)
- `src/domain/erstatningsopgoerelse/snapshot/eoPresentationModel.ts` (147 l.)
- `src/domain/erstatningsopgoerelse/snapshot/eoPresentationSectionBuilders.ts` (413 l.)
- `src/domain/erstatningsopgoerelse/snapshot/eoSnapshotInvariants.ts` (151 l.)
- `eoSnapshotToBeregningView.ts`, `eoSnapshotToEoPdfDocument.ts`, `eoSnapshotToTafPerYearPdfDocument.ts`, `eoSnapshotToDebugView.ts`
- Kontrol mod: `eo-snapshot-contract.md`, `contract-topology.json`, `contractCoverageMatrix.test.ts`.

**Tests kørt:** `npx vitest run` → 362 filer / 4501 passed. `npx tsc --noEmit` → 0 fejl.

## Fund og rettelser

1. **Høj (konvergens / Single Source of Truth — kontrakt §1) — EO-totaler blev beregnet parallelt to steder.** `buildEoComputedTotals` (`eoCanonicalOutput.ts:108-149`) og `buildErstatningsopgoerelsePdfModelFromComputed` (`eoPresentationModel.ts`) genberegnede uafhængigt de samme totaler (forlig-skalering → fradrag af `tidligereModtagetTaf` → clamp → sum) med identisk formel. To implementeringer af samme beregning er en latent drift-fælde, og kontrakt §1 forbyder eksplicit: "Ingen EO-total må beregnes parallelt i UI-komponenter, PDF-writers eller debug-lag".
   - ✅ **Rettet** — `buildErstatningsopgoerelsePdfModelFromComputed` modtager nu de autoritative `EoComputedTotals` fra orkestreringen (`eoSnapshot.ts` har dem allerede i scope) og **læser** dem i stedet for at genberegne. PDF-modellen har ingen egen total-beregning længere. Fjernede de nu-ubrugte `clampMoneyOreToZero`/`ensureMoneyOre`/`scaleMoneyOre`-imports. **Behavior-equivalent** (identisk formel → identiske tal) — bekræftet af 4501 grønne tests + grøn typecheck.

2. **1.7-henført forhold — EO-snapshot-kontraktens abstraktionsasymmetri (§2.3, detaljeret TAF-/svie-behandlingsrækkefølge ~80 linjer).** 1.7 anbefalede at flytte den trinvise pipeline-spec til `docs/architecture/` som informativ doc og lade kontrakten beholde invarianterne, så abstraktionsniveauet matcher de øvrige domæne-kontrakter (41-68 l.).
   - ✅ **Rettet** — oprettet `docs/architecture/eo-clamping-pipeline-architecture.md` (informativ) med den fulde trinvise behandlingsrækkefølge for TAF og svie/smerte. Kontraktens §2.3 er nu en kort normativ sektion med **6 bindende invarianter** (validering-før-relevans, fejlgivende-før-stille-clamping, række-bundne løse feriedage pre-merge, centraliseret merge, ethvert svie/smerte-overlap afvises, ingen parallelle fallback-totaler) der henviser til pipeline-doc'en. Registreret den nye doc i `contract-topology.json`s `informativeArchitectureDocs` (coverage-matrix-test grøn). Ingen UI/UX- eller beregningslogik-berøring.

## Tilfældighedsfund

1. **Verificerede korrekte/bevidste forhold (subagent foreslog ændring; bevaret):**
   - **`eoSnapshot.ts` orkestreringen** wrapper engine-kald i try/catch og fail-closer med blokerende system-invariant + tom projektion ved runtime exception (kontrakt §3.3); clamping muterer aldrig committed state (§2). Korrekt.
   - **`eoSnapshotInvariants.ts`** matcher kontrakt §3 (`blocksAuthoritativeComputation`/`blocksOutputs`/source-klassificering); importerer `TAF_OVERLAP_ERROR_MESSAGE` fra validator (ikke dead). Projektionerne respekterer `getBlockingInvariantsForOutput` pr. target.
   - **Brutto vs. netto-kommentar** (`eoPresentationSectionBuilders.ts`): subagent kaldte den "misvisende", men feltet er korrekt brutto (fradrag sker downstream); kommentaren er præcis nok. Ikke ændret.
2. **Strukturel observation (ikke rettet):** `eoSnapshot.ts` (461 l.) og `eoPresentationSectionBuilders.ts` (413 l.) er split-kandidater, men kohæsive; noteret til evt. senere dedikeret split frem for ad hoc.

## Sammenfatning

- EO-snapshot-laget er korrekt og kontraktkonformt: én autoritativ entry, fail-closed runtime-håndtering, invariant-gating pr. output.
- Rettet: den parallelle EO-total-beregning i PDF-modellen er elimineret — PDF læser nu de autoritative canonical totals (SSoT, kontrakt §1). Behavior-equivalent, testbekræftet.
- 1.7-henført forhold lukket: den detaljerede TAF-/svie-pipeline er flyttet til en informativ `docs/architecture/`-doc, kontrakten beholder de 6 bindende invarianter — abstraktionsniveauet er nu på linje med de øvrige domæne-kontrakter.
- Punkt 4 (4.1-4.13) er hermed gennemført; ingen beregningsresultater ændret.
