# Punkt: 10.3 PDF-domæner I: eo (+ sections), differencekrav, eet, kapitalisering, loebendeYdelser

**Dato:** 2026-06-02
**Filer gennemgået:**
- `src/pdf/domains/eo/erstatningsopgoerelsePdf.ts`, `reguleringPdf.ts`, `types.ts`
- `src/pdf/domains/eo/sections/`: `opgoerelseSection.ts`, `reguleringSection.ts`, `loenindkomstSection.ts`, `offentligeYdelserSection.ts`, `shDageSection.ts`
- `src/pdf/domains/differencekrav/differencekravPdf.ts`
- `src/pdf/domains/eet/eetEfterEalPdf.ts`, `eetPdfUtils.ts`
- `src/pdf/domains/kapitalisering/kapitaliseringPdf.ts`
- `src/pdf/domains/loebendeYdelser/loebendeYdelserPdf.ts`
- Kontrakter: `pdf-contract.md` (§3 toggle-guards, §4 semantisk fravalg, §7), `pdf-layout-contract.md`; arkitektur: `pdf-architecture.md` (§12, §15, EO model-renderer-split)

**Tests kørt:** `typecheck` grøn; EO-relaterede tests kørt samlet i fuld suite for 10.1-10.5.

## Fund og rettelser

1. **Lav — duplikeret `capitalizeFirstChar`/`toSentenceCase` med locale-inkonsistens.**
   `capitalizeFirstChar` (`erstatningsopgoerelsePdf.ts`) og `toSentenceCase` (`reguleringSection.ts`) var identiske (`toLocaleUpperCase('da-DK')`), mens et tredje kaldested i `reguleringSection.ts` brugte locale-uafhængig `.toUpperCase()`.
   - ✅ **Rettet:** Indført kanonisk `capitalizeFirstCharDa` i `src/utils/formatUtils.ts`; begge lokale helpers fjernet og alle tre kaldesteder bruger nu den kanoniske (locale-konsistent `da-DK`). For reelle danske strenge er output identisk; det locale-uafhængige kaldested rettes dermed til samme adfærd som de øvrige.

## Godkendt og rettet (model-renderer-split / beregningslogik-grænse)

2. **Medium — renderer genberegnede afrundet per-dag svie/smerte-sats.**
   `opgoerelseSection.ts` udregnede `roundByMethod(satserPerDag.value * delvisFaktor, 0, ...)` to steder (takst-tekst + "Beregnet krav").
   - ✅ **Rettet (godkendt af bruger):** Tilføjet `delvisSatsPerDag` og `delvisSatsPerDagFoerForlig` (`Calculable<MoneyOre>`) til `SvieSmerteModel`, beregnet i `eoPresentationSectionBuilders.ts` med samme afrunding som tidligere. Rendereren formaterer nu blot modelfeltet; de to lokale genberegninger fjernet. Output uændret (verificeret via EO-suite). Den øvrige ad hoc-runtime-afrunding `roundByMethod` i opgoerelseSection (deltaPct/maaneder) er præsentationsafrunding og uberørt.

3. **Medium — `reguleringSection.ts` genberegner domænedata forbi projektionen.**
   `resolveOverenskomstTillægsStigninger` m.fl. trækker direkte fra rates-laget og re-afleder hvilke tillæg (fritvalg/SH-SO/pension/store-bededag) der stiger, samt anciennitets-konvertering.
   - ⏭ **Ikke rettet (parkeret):** Dette er den mest omfattende model-renderer-split-flytning og berører hvilke ord der står i bilaget. Det blev ikke medtaget i denne ombæring for at holde godkendelses-rettelserne afgrænsede og verificerbare. Bør tages som et selvstændigt stykke arbejde (flyt "hvilke tillæg steg"-bestemmelsen + anciennitets-konvertering ind i projektionen, eksponér typed labels) med før/efter-regressionsverifikation. Forelægges på ny når det gribes an.

4. **Medium — divergerende dobbelt-rendering af `offentligeYdelserUdvikling`-segmenter (delta-faktor-afrunding).**
   Reguleringsfaktor-teksten (`x (100 % ± d %)`) blev bygget med rå `segment.deltaPct` i "Regulering af offentlige ydelser"-bilaget, men med 2-decimal-afrundet delta (og dermed undertrykkelse ved near-nul) i hovedopgørelsen. Konkret divergens: ved en near-nul-regulering viste bilaget `"x (100 % + 0,00 %)"`, mens hovedopgørelsen undertrykte faktoren.
   - ✅ **Rettet (godkendt af bruger — valgte "skjul ved 0,00 %"):** Indført kanonisk `formatReguleringFactorText(deltaPct)` i `pdfFormatUtils.ts` (afrunder til 2 decimaler, undertrykker faktoren når den runder til 0,00 %). Brugt på alle tre segment-linje-steder (hovedopgørelse, offentlige-ydelser-bilag, TAF-fordelt — se 10.4-fund 6), så faktorteksten nu er ens overalt. Tests tilføjet. Bilaget skifter fra `"x (100 % + 0,00 %)"` til ingen faktor ved near-nul.

## Verificeret OK (ikke-handlede fund)

- **Toggle/render-guards (§3):** Alle betingede bilag-sektioner er korrekt guardede (`selectedElements.*`, `skalViseIndkomstOgYdelserBilag`, `hasLoenReguleringEoBilagData`, `midlertidigtEetGroups.length > 0`, `regulerOffentligeYdelser === 'Ja'`, `entries.length > 0`, `forlig.erIndgaaet`, `beregnes`-flags). `assertModelInvariant` håndhæver `harPerioder`/`harTafPerioder`. Intet felt udskrives uden guard — ingen stale-værdi-risiko fundet.
- **Semantisk fravalg (§4):** Renderere respekterer `beregnes === false` (svie/smerte, TAF skriver "Ingen" frem for at genindføre linjer); `loenudviklingBeregningsgrundlag === 'Ingen'` honoreres. Ingen visnings-only-genindførsel af fravalgt indhold.
- **jsPDF/writer (§7-§8):** Ingen direkte `doc.text`/`setFont` for almindelig tekst; `getDoc()` sendes kun til `renderPdfTable`; `didParseCell`/`didDrawCell` er de eneste direkte-doc-kontekster (tilladt). Alle tabeller via `renderPdfTable`; sektionsafslutninger via `resolvePdfSectionEndY`.
- **Currency/format:** Al monetær formatering via kanoniske helpers; `formatCurrencyFromOre` guarder `!Number.isFinite`.
- **Robusthed:** `differencekravPdf` tom-afgørelse håndteret; `offentligeYdelserSection` guarder tomme arrays, `parseISODate`-fejl, `<= 0`-beløb og division-med-0 (`maanedligYdelse > 0`); `reguleringPdf` guarder `toLoentrin` via try/catch og gruppe-range; `buildOffentligeYdelserReguleringTableData` wrappet i try/catch med brugervenlig fallback (korrekt §5 — forventelig gate-tilstand).
- **`reguleringPdf` index-afrunding:** Præsentationsafrunding af allerede-autoritativt indeks (ikke money) — acceptabelt.

## Tilfældighedsfund

- Mindre konvergens-dubletter (`buildEetYdelserHeaderRow` bygget to steder i `offentligeYdelserSection`/`loebendeYdelserPdf`; near-dup uppercase-helper) — den ene del løst via `capitalizeFirstCharDa` (fund 1); EET-header-dubletten noteret men ikke handlet (lav værdi, kan slås sammen ved næste EET-berøring).

## Sammenfatning

- Pure-code: locale-konsistent kanonisk `capitalizeFirstCharDa` indført og brugt; locale-inkonsistens rettet.
- Godkendt og rettet: svie/smerte-per-dag-sats flyttet til præsentationsmodellen (fund 2); kanonisk `formatReguleringFactorText` indført og brugt på alle tre segment-linje-steder med forenet near-nul-undertrykkelse (fund 4).
- Parkeret: den store regulerings-tillægs-bestemmelse i `reguleringSection.ts` (fund 3) — selvstændigt arbejde, forelægges på ny når det gribes an.
- Guard-fladen (§3) og semantisk fravalg (§4) verificeret intakt — ingen stale-værdi-risiko. Punktet er konvergeret pånær det parkerede fund 3.

---

## Genbesøg 2026-06-04 (efter commits 30330062..HEAD)

**VERDIKT: Skal opdateres (guard-mønster ændret fra 2-tilstand til 3-tilstand; tidligere konklusion holder fortsat for det den dækkede).** Drift-doc'en markerede 10.3 "ufuldstændig + delvist stale": `beregnes`-booleans afløst af en tre-tilstands-enum (`jaNejSkjulEnum`) for svie/smerte, TAF og øvrige krav, plus `afsluttesMed === 'Ingen'`.

**Hvad er stadig sandt:** Fund 1, 2 og 4 (kanoniske helpers, svie/smerte-per-dag-sats fra modellen, `formatReguleringFactorText`) er uændrede og verificeret stadig på plads i `opgoerelseSection.ts`. Fund 3 forbliver parkeret. §4 (semantisk fravalg) holder.

**Ny 3-tilstands-skjul-guard — korrekthedsvurdering (KORREKT):**
`opgoerelseSection.ts` implementerer det tre-tilstands-mønster drift-doc'en beskriver, og det er konsistent med §3:
- **Skjul:** `if (!model.X.skjul) { … }` omslutter hele emnet (overskrift + indhold). `skjul = enum === 'Skjul'` (afledt i `eoPresentationSectionBuilders.ts`). Resultat: ingen overskrift, intet "Ingen". Emnets linje i "Samlet erstatningskrav" er ligeledes guardet med `if (!model.X.skjul)`, og `model.samlet.*Ore` er 0 for et skjult emne (`beregnes === false` ⇒ totalOre 0), så "Erstatningskrav i alt" forbliver korrekt.
- **Nej:** `beregnes = enum === 'Ja'` (svie/smerte kræver dertil `tidligereSsMax === 'Nej'`). Ved 'Nej' er `skjul` false og `beregnes` false ⇒ overskrift renderes + `safeAddWrappedText('Ingen')`. Summary-linjen vises med 0 kr.
- **Ja:** indhold renderes.
- **Ingen stale-værdi-printning:** Alle felter skrives fortsat kun inden for guards; ingen rå-værdi udskrives uden for de tre tilstande. `assertModelInvariant(harPerioder === (periodeLinjer.length>0))` m.fl. er bevaret.

**"Ingen"-afsluttesMed-sti (KORREKT):** Efter "Samlet erstatningskrav" (+ evt. "Særlige bemærkninger") returnerer rendereren `if (afsluttesMed === 'Ingen') return;` **før** "Godkendelse"-overskriften. 'Bekræftet godkendt' og 'Underskrift-linje' renderer fortsat hver sin godkendelses-tekst (sidstnævnte med signaturblok). Ingen lækage af signatur-/godkendelses-indhold ved 'Ingen'.

**Tekst/struktur-ændringer i forventet indkomst:** "én samlet I alt" (præcis ÉN afsluttende I alt-linje på tværs af løn + offentlige ydelser, vist kun ved >1 segment; udelades hvis en kilde ikke kan beregnes) er guardet korrekt — ingen vildledende delsum.

**Tests:** `erstatningsopgoerelsePdf.skjulEmner.test.ts` dækker Nej (overskrift) vs. Skjul (udeladt), per-emne-isolation, og Samlet-sektionens bevarede "Erstatningskrav i alt". **Tilføjet:** test af `afsluttesMed === 'Ingen'`-stien (udelader "Godkendelse"; "Underskrift-linje"/"Bekræftet godkendt" beholder den). `npx vitest run src/__tests__/utils/pdf/` grøn; `typecheck:test` grøn.

**FORELÆG TIL BRUGER (brugervendt PDF-output):**
- **"Ingen"-stien:** Et nyt afslutnings-valg udelader hele "Godkendelse"-afsnittet fra erstatningsopgørelsen. Bekræft at det er den ønskede adfærd (intet godkendelses-/underskriftafsnit i PDF'en når brugeren vælger "Ingen").
- **Skjul vs. Nej:** "Skjul" fjerner emnet helt fra PDF'en (også linjen i det samlede krav); "Nej" beholder overskrift + "Ingen" og en 0 kr.-linje i det samlede krav. Bekræft denne sondring er som ønsket.
- **"én samlet I alt" i Forventet indkomst:** Tidligere kunne der vises delsummer pr. kilde; nu vises kun én samlet I alt-linje. Kosmetisk strukturændring i et brugervendt dokument — bekræft.
- **Kommentarfelt i offentlige-ydelser-bilag:** Se 10.4-genbesøg (ny `offentligeYdelserKommentarer` rendres i bilaget).
