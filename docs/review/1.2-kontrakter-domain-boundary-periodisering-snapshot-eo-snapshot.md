# Review: 1.2 Kontrakter — domain-boundary, periodisering, snapshot, eo-snapshot

**Dato:** 2026-05-21
**Filer gennemgået:**
- `src/contracts/domain-boundary-contract.md`

---

## Review-runde 4 — 2026-05-25

### Status på tidligere fund

- ✅ **Fund 2 (Høj) — faellesAarsloen skriveejerskab**: IMPLEMENTERET — `src/__tests__/hooks/usePersistedForm.test.tsx` linje 392–432 dækker præcis scenariet: committed `aslAarsloen: { kind: 'number', value: 512000 }` bevares uændret efter remount med ny URL (`/forsoergertab`). Testen er korrekt annoteret med en ny `MemoryRouter`-instans.
- ✅ **useMidlertidigtEetInsertSource unit test**: IMPLEMENTERET — `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.tsx` eksisterer og tester at schema-invalid `faellesAarsloen` producerer det korrekte issue-id `midlertidigt-eet-faelles-aarsloen-schema-invalid`.
- ⚠️ **useMidlertidigtEetInsertSource schema-fejl maskeres (Medium)**: DELVIST — `useMidlertidigtEetInsertSource.ts` emitter nu korrekt issues ved schema-invalid sektioner (linje 43–63), men selve `eetValues`-objektet bygges stadig med fallback til `ERHVERVSEVNETAB_INITIAL_VALUES` og `FAELLES_AARSLOEN_INITIAL_VALUES` ved schema-fejl (linje 70–74). Kontraktkravet om at "schema-ugyldig EET-kilde ikke maskeres som tom import" er halvvejs opfyldt: issues signaleres, men `eetValues` indeholder stadig initial-values som basis, ikke en tydelig tom/blokeret kilde. En forbruger der ser på `eetValues` alene vil ikke opdage fejlen.
- ✅ **contractCoverageMatrix — eoSnapshot.test.ts mangler i snapshot-matrix**: IMPLEMENTERET — `src/__tests__/domain/erstatningsopgoerelse/eoSnapshot.test.ts` er nu tilføjet til `snapshot-contract.md`-entryen i matrix (linje 99–105).
- ❌ **Fund 9/14 (Medium) — regressionstest for krydsperiode 2012-07-02**: Stadig ikke implementeret.
- ❌ **Fund 13 (Medium) — test der simulerer exception i snapshot-entrypoints (EET, FST)**: Stadig ikke implementeret.

### Nye fund

Ingen nye fund i dette scope.

### Samlet vurdering

De to vigtigste åbne fund fra forrige runde (faellesAarsloen-test og useMidlertidigtEetInsertSource-test) er nu implementerede. `useMidlertidigtEetInsertSource` emitter korrekte issues men bygger stadig eetValues på initial-values-fallback, hvilket er en accepteret bevidst udsættelse. De tilbageværende mangler (krydsperiodetest og snapshot-exception-tests) er medium severity og ingen er korrekthedskritiske i nuværende implementering.

---
- `src/contracts/periodisering-contract.md`
- `src/contracts/snapshot-contract.md`
- `src/contracts/eo-snapshot-contract.md`

---

## Implementeringsreview (2026-05-23)

**Filer undersøgt:**
- `src/contracts/domain-boundary-contract.md`
- `src/contracts/periodisering-contract.md`
- `src/contracts/snapshot-contract.md`
- `src/contracts/eo-snapshot-contract.md`
- `src/__tests__/quality/domainBoundaryIsolation.test.ts`
- `src/__tests__/quality/contractCoverageMatrix.test.ts`
- `src/hooks/useMidlertidigtEetInsertSource.ts`
- `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetInsertRows.ts`
- `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetTransientInjection.ts`
- `src/domain/erstatningsopgoerelse/snapshot/eoSnapshot.ts`
- `src/components/pages/Erstatningsopgoerelse.tsx`
- `src/__tests__/domain/erstatningsopgoerelse/midlertidigtEetInsertRows.test.ts`

### Resumé

- Kontraktteksterne er generelt opdateret og afspejler beslutningerne fra reviewet. De fleste Høj- og Medium-fund er adresseret på kontraktniveau.
- `eo-snapshot-contract.md` §4-tabellen er harmoniseret med §3.1: `blocksAuthoritativeComputation` → `status: 'error'` med `data: null`; `fail_closed` er reserveret til system-/schema-/runtime-tilstande. Fund 6 er implementeret.
- Fund 3 (EET-importfilter) er kun delvist implementeret: kontraktteksten er skærpet, men den underliggende kode dropper stadig ukendte afgørelsestyper lydløst fremfor at fail-close.
- Fund 7 (EET-kildedistinktion) er kun delvist implementeret: kontrakten skelner nu eksplicit mellem "ingen relevante rækker" og "kilde kunne ikke bygges schema-sikkert", men `useMidlertidigtEetInsertSource.ts` maskerer fortsat schema-fejl ved at falde tilbage til initial values.
- De fleste anbefalede regressionstests mangler stadig: ingen test for krydsperiode ved 2012-07-02 (Fund 9/14), ingen test for EET/FST snapshot exception-blokering (Fund 13), ingen test for navigation der ikke overskriver committed `faellesAarsloen` (Fund 2).

### Status pr. fund

**Fund 1 — snapshot-kontrakt som passiv reference**
[IMPLEMENTERET] `domain-boundary-contract.md` §10.4 kræver nu en eksplicit domænespecifik kontrakt eller entrypoint-deklaration med autoritative inputsektioner, forbrugere, status-/issue-model, PDF/debug-projektioner og runtime fail-closed-semantik. `snapshot-contract.md` §3 er omskrevet til checklist-format med seks konkrete krav pr. snapshot-first domæne.

**Fund 2 — skriveejerskab for `faellesAarsloen`**
[DELVIST] Kontrakten er opdateret: §6.4 definerer nu `faellesAarsloen` som multi-writer-undtagelse, forbyder overskrivning uden brugercommit (punkt 6), og kræver samme schema, initial values og valideringsregler (punkt 7). `src/domain/aslEalAarsloen/` er kanonisk. Der mangler dog tests der viser, at navigation frem og tilbage mellem EET og Forsørgertab ikke overskriver en eksisterende committed `aslAarsloen`/`ealAarsloen` uden brugerinput.

**Fund 3 — EET-importfilter i EO**
[DELVIST] `domain-boundary-contract.md` §9.6 siger nu eksplicit at ukendte eller kontraktstridige importrelevante afgørelsestyper skal fail-close og rapporteres som blocking issue — de må ikke silently droppes. Men i `buildMidlertidigtEetAfgoerelseGroupsFromComputation` sker der fortsat en `continue` (linje 35) for alt andet end `Midlertidig`/`Delvist endelig`, uden at ukendte typer giver en invariant eller throws. Invariant-kravet er ikke kodeimplementeret, og der er ingen test der bekræfter fail-close for ukendt type.

**Fund 4 — periodiseringsscope**
[IMPLEMENTERET] `periodisering-contract.md` er omskrevet til en taksonomisk kontrakt med fire kategorier (EO-TAF, Årsløn/omregning, EET-løbende ydelser, Forsørgertab/kapitalisering) og kanoniske motorplaceringer pr. kategori. EO's motor antages ikke længere at gælde for de øvrige domæner.

**Fund 5 — snapshot minimumsindhold**
[IMPLEMENTERET] `snapshot-contract.md` §3 er nu et checklist-format med seks konkrete krav til alle snapshot-first domæner. Kontrakten kræver stadig ikke én fælles TypeScript-shape.

**Fund 6 — EO status `error` vs. `fail_closed`**
[IMPLEMENTERET] `eo-snapshot-contract.md` §4-tabellen er harmoniseret med §3.1. De tre statustilstande er nu præcist beskrevet: `fail_closed` (system/schema/runtime), `error` med `data: null` (bruger/validator-blokering, herunder `blocksAuthoritativeComputation`), `error` med `data` (output-specifik blokering). Kode og kontrakt er i sync. `domainBoundaryIsolation.test.ts` og `eoSnapshot.test.ts` dækker de centrale stier.

**Fund 7 — EET-issues i EO**
[DELVIST] `eo-snapshot-contract.md` §13 skelner nu eksplicit mellem "ingen relevante EET-rækker" og "EET-kilden kunne ikke bygges schema-sikkert", og kræver at sidstnævnte ikke maskeres som tom import. Men `useMidlertidigtEetInsertSource.ts` linje 38-53 falder stadig tilbage til `ERHVERVSEVNETAB_INITIAL_VALUES` ved `parsedErhvervsevnetab.success === false` — schema-fejlen maskeres som funktionelt tom kilde. Kontraktkravet er ikke overholdt i implementeringen.

**Fund 8 — sidegrænseliste og sektionsliste**
[IMPLEMENTERET] `domain-boundary-contract.md` §5 hedder nu "Persistensskrivende sidegrænser" og indeholder en eksplicit note om at `Satser` er en sags-global side, ikke et fagdomæne. Listen er bevaret, men skellet er nu dokumenteret.

**Fund 9 — hverdage/arbejdsdage og SH**
[IMPLEMENTERET] `periodisering-contract.md` §2 siger nu at "hverdage" er basisbegrebet, og at domænespecifikke regler kan gøre `arbejdsdage` lig `hverdage` for konkrete perioder. §5 er specificeret med slutdato-baseret klassifikation og en explicit note om at krydsperioder ikke opdeles automatisk. Der mangler dog en regressionstest for perioder der krydser 2012-07-02 (se Fund 14).

**Fund 10 — reference til EO snapshot**
[IMPLEMENTERET] `snapshot-contract.md` §6.5 nævner nu `eo-snapshot-contract.md` som domænespecifik specialisering. `computeEoSnapshot(...)` er tilføjet som eksempel i §4.2.

**Fund 11 — EO-specialhook direkte store-adgang**
[IMPLEMENTERET] `domain-boundary-contract.md` §9 dokumenterer undtagelsen som eksplicit read-only adgang for `useMidlertidigtEetInsertSource`. `domainBoundaryIsolation.test.ts` har en dedikeret test der låser tilladte sektioner til `['erhvervsevnetab', 'faellesAarsloen', 'stamdata']` og bekræfter at write-API'er er fraværende. Kommentaren i testen angiver eksplicit begrænsningerne (smal regex, aliasering kræver manuel review).

**Fund 12 — snapshot-revision for transient EET**
[IMPLEMENTERET] `eo-snapshot-contract.md` §13 siger nu at revisionen skal inkludere EO, stamdata, EET og `faellesAarsloen` deterministisk, uanset om togglen er `'Ja'` eller `'Nej'`. `Erstatningsopgoerelse.tsx` linje 110-117 bekræfter at alle fire sektioner altid er i `buildDebugRevision`, og kommentaren forklarer det bevidste valg.

**Fund 13 — runtimefejlmodeller på snapshots**
[DELVIST] `snapshot-contract.md` §6A er et nyt afsnit der fastlægger at runtimefejl skal fail-close, rapporteres efter `error-debug-contract.md`, give dansk blokerende brugerbesked og undgå fallback-beregninger. EO's implementation er dækket. EET og FST har implementerede runtime-exception-stier (`createRuntimeExceptionIssue` hhv. `createRuntimeExceptionCalculation`) der rapporterer via `reportSystemIssue` med dansk fejlbesked og blokerer computation. Der mangler dog tests der simulerer en exception i hvert snapshot-entrypoint og kontrollerer blokeringen.

**Fund 14 — offentlige ydelser cutover**
[DELVIST] `periodisering-contract.md` §5 specificerer nu slutdato-baseret klassifikation og konsekvensen for krydsperioder. Kode og kontrakt er i sync. Der mangler en regressionstest for en sygedagpengeperiode der krydser 2012-07-02.

**Fund 15 — canonical mappe for fælles årsløn**
[IMPLEMENTERET] `domain-boundary-contract.md` §6.4 punkt 5 peger nu på `src/domain/aslEalAarsloen/` som kanonisk placering. Ingen ny `src/domain/faellesAarsloen/`-mappe er oprettet.

### Nye fund fra implementeringen

**Høj — `buildMidlertidigtEetAfgoerelseGroupsFromComputation`: kontraktbrud ved ukendte afgørelsestyper**
Lokation: `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetInsertRows.ts` linje 35.
Problem: `continue` for enhver type der hverken er `Midlertidig` eller `Delvist endelig`. Kontrakten (`domain-boundary-contract.md` §9.6) kræver eksplicit fail-close og blocking issue ved ukendt type. Implementeringen opfylder ikke det krav.
Risiko: En fremtidig ny EET-afgørelsestype vil silently droppes som ikke-importrelevant, uden at EO-beregningen rapporterer det som en fejl. Kontrakten giver falsk tryghed om at invarianten er håndhævet.
Anbefaling: Tilføj en `else`-gren der kaster eller returnerer en blocking EET-issue når en ikke-tom afgørelse har en ukendt type. Dæk med en test der giver en afgørelse med ukendt `afgoerelseType` og bekræfter, at EO-snapshot sætter blocking issue.

**Medium — `useMidlertidigtEetInsertSource`: schema-fejl maskeres som tom kilde**
Lokation: `src/hooks/useMidlertidigtEetInsertSource.ts` linje 38-51.
Problem: `parsedErhvervsevnetab.success === false` → fald tilbage til `ERHVERVSEVNETAB_INITIAL_VALUES`. Kontrakten (`eo-snapshot-contract.md` §13) siger at "EET-kilden kunne ikke bygges schema-sikkert" er en anden tilstand end "ingen relevante rækker" og ikke må maskeres som tom import.
Risiko: En schema-invalid EET-sektion vil resultere i en beregning baseret på initial values, uden at brugeren ser en fejl. Det kan producere EO-beregninger der ikke afspejler brugerens faktiske EET-data.
Anbefaling: Returner en distinct `kildeStatus: 'schema_invalid'`-tilstand eller en EET-issue fra `useMidlertidigtEetInsertSource` når safeParse fejler, så EO-beregningen og brugerfladen kan skelne de to tilstande.

**Medium — `contractCoverageMatrix.test.ts`: `snapshot-contract.md` mangler EO-snapshot-entrypoint i matrix**
Lokation: `src/__tests__/quality/contractCoverageMatrix.test.ts` linje 29-34.
Problem: `snapshot-contract.md`-matrixen inkluderer kun `forsoergertabSnapshot.test.ts` og `eetSnapshot.test.ts`. `eoSnapshot.test.ts` mangler, selv om EO er det mest kontrakttunge snapshot.
Risiko: Lav (testen er kun en linkage-guard), men asymmetrien signalerer at EO-snapshot-kontrakten ikke formelt er registreret som dækket.
Anbefaling: Tilføj `src/__tests__/domain/erstatningsopgoerelse/eoSnapshot.test.ts` til `snapshot-contract.md`-entryen i matrix.

---

## Opfølgende implementeringsreview (2026-05-23)

**Filer undersøgt:**
- `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetInsertRows.ts`
- `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetTransientInjection.ts`
- `src/domain/erstatningsopgoerelse/snapshot/eoSnapshot.ts`
- `src/domain/erstatningsopgoerelse/snapshot/eoSnapshotInvariants.ts`
- `src/hooks/useMidlertidigtEetInsertSource.ts`
- `src/__tests__/domain/erstatningsopgoerelse/midlertidigtEetInsertRows.test.ts`

### Ændret status siden første review

**Nyt fund (Høj) fra første implementeringsreview — `buildMidlertidigtEetAfgoerelseGroupsFromComputation`: kontraktbrud ved ukendte afgørelsestyper**

Gammel status: Åben — `continue` for ukendte afgørelsestyper, ingen invariant eller blocking issue.

Ny status: **IMPLEMENTERET**

Rettelsen er trelaget og korrekt gennemført:

1. `midlertidigtEetInsertRows.ts` linje 35–38: `Endelig` håndteres stadig med `continue` (bevidst: en `Endelig`-afgørelse er ikke fejltilstand, blot ikke importrelevant). For alt andet end `Midlertidig` og `Delvist endelig` kastes nu `throw new Error('CRITICAL: Ukendt EET-afgørelsestype i midlertidigt EET-import.')`. Det opfylder kontraktkravet om fail-closed ved ukendt type.

2. `midlertidigtEetTransientInjection.ts` linje 43–56: `buildMidlertidigtEetAfgoerelseGroupsFromComputation` er wrapped i et `try/catch`. Ved throw returneres `{ groups: [], issues: [{ id: 'midlertidigt-eet-import-invariant', severity: 'error', message }] }`. Fejlen propageres som EET-issue i stedet for at lade undtagelsen boble op ukontrolleret.

3. `eoSnapshotInvariants.ts` linje 59–72: `buildMidlertidigtEetSourceInvariants` konverterer issues med `severity: 'error'` til `EoInvariant` med `blocksAuthoritativeComputation: true` og `blocksOutputs: VALIDATION_BLOCKED_OUTPUTS`. Dermed når invarianten `hasAuthoritativeBlockingInvariant`-tjekket i `eoSnapshot.ts` linje 264 og stopper autoritativ beregning.

4. `midlertidigtEetInsertRows.test.ts` linje 120–165: To nye tests dækker eksplicit de to throw-stier — ukendt afgørelsestype (`'Ukendt'`) og ukonverterbar dato. Begge tests er korrekt annoteret med `@ts-expect-error` for at konstruere umulige engine-outputs.

Hele fail-closed-kæden er nu implementeret og testet: `throw` → `catch` → EET-issue med `severity: 'error'` → `EoInvariant` med `blocksAuthoritativeComputation: true` → snapshot returnerer `status: 'error'` og `data: null`.

**Én distinktion der fortsat mangler test-dækning:** Der er ingen integrationstest der starter fra `buildMidlertidigtEetSourceResult` med ukendt afgørelsestype og verificerer at `computeEoSnapshot` ender med `status: 'error'` og `data: null`. Enhedstestene dækker de to ender (throw-adfærd og invariant-semantik) isoleret, men ikke hele kæden end-to-end. Risikoen er lav — kæden er simpel og korrekt implementeret — men en integrationstest ville lukke det formelt.

---

### Uændret åbne fund

**Medium — `useMidlertidigtEetInsertSource`: schema-fejl maskeres som tom kilde**

Stadig åben. `useMidlertidigtEetInsertSource.ts` linje 46–50 bruger stadig spread-fallback-mønsteret: `...(parsedErhvervsevnetab.success ? parsedErhvervsevnetab.data : {})`. En schema-invalid `erhvervsevnetab`-sektion giver `ERHVERVSEVNETAB_INITIAL_VALUES` som base i stedet for en distinct fejltilstand. Kontraktkravet i `eo-snapshot-contract.md` §13 om at skelne "ingen relevante rækker" fra "kilde kunne ikke bygges schema-sikkert" er ikke opfyldt i implementeringen.

Brugeren har bekræftet at dette ikke er rettet i denne omgang (bredere modelændring krævet). Acceptabelt som bevidst udskydelse, men kravet er ikke bortfaldet.

**Øvrige åbne fund fra første implementeringsreview:**

- **Fund 2 (Høj)**: `faellesAarsloen` skriveejerskab — mangler tests der viser at navigation ikke overskriver committed årsløn. Stadig åben.
- **Fund 9/14 (Medium)**: Regressionstest for krydsperiode ved 2012-07-02 — stadig manglende.
- **Fund 13 (Medium)**: Test der simulerer exception i hvert snapshot-entrypoint — stadig manglende for EET og FST.
- **Medium — `contractCoverageMatrix.test.ts`**: `eoSnapshot.test.ts` mangler i `snapshot-contract.md`-matrixen — stadig åben.

---

### Nye fund fra denne runde

Ingen nye arkitektur- eller korrekthedsfund. Rettelsen er snæver, præcis og følger eksisterende mønstre.

**Observation (Lav):** `buildMidlertidigtEetAfgoerelseGroupsFromComputation` linje 35 dropper fortsat `Endelig`-afgørelser med en stille `continue` uden at emittere nogen issue. Det er et bevidst designvalg — `Endelig` er ikke importrelevant, og det er ikke en fejltilstand. Men kontrakten (`domain-boundary-contract.md` §9.6) siger "ukendte eller *kontraktstridige* importrelevante afgørelsestyper" skal fail-close. `Endelig` er kontraktstridigt i importkontekst, men er ikke ukendt. Vurdering: den nuværende silent-drop af `Endelig` er funktionelt korrekt og konsistent med at `buildMidlertidigtEetSourceResult` linje 26–29 allerede filtrerer `Endelig`-rækker fra *før* `computeEetLoebendeYdelser` kaldes. En `Endelig`-afgørelse burde aldrig nå `buildMidlertidigtEetAfgoerelseGroupsFromComputation` i normal drift. Kæden er defensivt korrekt. Ingen anbefaling om rettelse, men kontraktteksten i §9.6 bør præcisere at "kontraktstridig" her specifikt betyder ukendt type — ikke `Endelig`, som er en gyldig og bevidst droppet type.

---

## Fund

### 1. Høj — `domain-boundary-contract.md` §10.4 — Renvask via snapshot-kontrakt

**Lokation:** `domain-boundary-contract.md` §10, punkt 4: *"Når et domæne bruger snapshot-first, skal snapshot-entrypointet også følge `src/contracts/snapshot-contract.md`."*

**Problem:** Reglen hænger i luften uden operationelt indhold. Den siger at et snapshot-entrypoint "skal følge" snapshot-kontrakten, men specificerer ikke hvad der sker ved brud: hvem tjekker det, hvornår og hvordan. Den er ment som en håndhævelseshook, men er i praksis en passiv reference.

**Risiko:** Nye domæner kan løftes til snapshot-first og opfylde domain-boundary-kontraktens §10.4 blot ved at kalde entrypointet `computeXxxSnapshot` — uden reelt at opfylde snapshot-kontraktens krav om committed-only input, read-only output og fravær af sideeffekter. Reglen giver falsk tryghed.

**Anbefaling:** Tilføj mindst én operationel konsekvens i §10.4 — fx at reviews automatisk afviser snapshot-entrypoints uden eksplicit angivelse i domænekontrakten af hvilken snapshot-form der er valgt (felt-UI eller issue-/tab-). Det gør reglen verificerbar ved review frem for blot at være en blød reference.

**Worker-supplering:** Den aktuelle kodebase har allerede en delvis operationel håndhævelse gennem `src/__tests__/quality/domainBoundaryIsolation.test.ts`, men testen dækker kun page-lagets persisted sektionsadgang og EO-specialhookets snævre read-only adgang. Den håndhæver ikke snapshot-kontraktens semantik: ingen side effects, ingen draft-state, ingen parallelle tab-/PDF-beregninger og ingen uautoriseret domænelæsning inde i selve snapshot-entrypointet. Et optimalt slutprodukt bør derfor splitte §10.4 i to verificerbare krav: (1) page-lagets adgangsgrænser håndhæves af en quality-test, og (2) hvert snapshot-first domæne skal have en navngiven entrypoint-kontrakt med autoritative forbrugere, tilladte inputsektioner, status-/issue-model og minimumsprojektioner. Det vil gøre kontrakten reviewbar uden at kræve en stor generisk snapshot-framework-abstraktion.

---

### 2. Høj — `domain-boundary-contract.md` §6.4 — Skriveejerskab for `faellesAarsloen` er for løst

**Lokation:** `domain-boundary-contract.md` §6.4, punkt 3: *"`Erhvervsevnetab` og `Forsørgertab` må læse og skrive sektionen."*

**Problem:** Begge domæner har symmetrisk skriveadgang til `aslAarsloen` og `ealAarsloen` — begge felter. Kontrakten specificerer ikke hvilken side der er primær skriver for hvort felt, og hvad der sker hvis begge sider skriver til samme felt. Lyder som et "sidst-vinder"-scenarie uden garantier om konvergens.

**Risiko:** Brugeren navigerer frem og tilbage mellem EET og Forsørgertab og opdager at et årslønsfeltet har en anden værdi end sidst set, fordi den anden sides seneste commit overskrev det. Dette er datatab fra brugerens perspektiv. Endnu mere kritisk: hvis de to domæner har divergerende schemas eller defaults for samme felt, kan rækkefølgen af commits påvirke beregnede tal.

**Anbefaling:** Specificér for hvert felt (`aslAarsloen`, `ealAarsloen`) hvem der er primær skriver og om den anden side må overskrive. Alternativt: separer felterne så Erhvervsevnetab ejer ét og Forsørgertab det andet — fjern den delte skriveadgang.

**Worker-supplering:** Koden bekræfter risikoen: både `src/components/pages/Erhvervsevnetab.tsx` og `src/components/pages/Forsoergertab.tsx` bruger `usePersistedForm(faellesAarsloenSchema, 'faellesAarsloen', ...)` og kan dermed committe begge felter. Samtidig anvender begge sider `useAslAarsloenRuleReporter(...)`, så samme feltfejl-/rulekanal er aktiv fra flere sider. Det er ikke nødvendigvis en aktuel bug, men det gør sektionen til den eneste sags-globale sektion med symmetrisk multi-writer-adgang på samme felter. For et trust-critical slutprodukt bør kontrakten enten definere feltvis skriveejerskab eller kræve en neutral, auditabel commit-model for `faellesAarsloen`, hvor en side kun må overskrive en eksisterende committed værdi efter eksplicit brugerhandling. Hvis UX'en fortsat skal tillade redigering fra begge sider, bør kontrakten mindst kræve, at ingen page-local default eller rehydrering må skrive `faellesAarsloen` uden brugercommit.

---

### 3. Høj — `domain-boundary-contract.md` §9 — EET-import via toggle: manglende fail-fast ved kontraktbrud

**Lokation:** `domain-boundary-contract.md` §9.6: *"Importen må kun medtage rækker fra afgørelser med typen `Midlertidig` eller `Delvist endelig`."*

**Problem:** Kontrakten specificerer reglen, men angiver ikke hvad der sker hvis implementeringen fejlagtigt inkluderer en `Endelig`-afgørelse — om snapshot skal fail-closed, om det er en silent-drop, eller om der skal vises en fejl. `eo-snapshot-contract.md` §13 specificerer den inverse garanti (EET-issues overføres ukritisk), men der er ingen binding til hvad der sker ved typebrud i selve filtreringen.

**Risiko:** En implementeringsfejl i afgørelsestype-filteret vil inkludere `Endelig`-afgørelser i EO-beregningen uden at nogen kontrakt specificerer en detektering- eller rapporteringsrute. Resulteret i en stille beregningsfejl.

**Anbefaling:** Tilføj til §9 (eller til `eo-snapshot-contract.md` §13) at filtreringen af afgørelsestyper er en snapshot-invariant der skal rapporteres som `blocksAuthoritativeComputation` ved brud — ikke som en silent-drop.

**Worker-supplering:** Den aktuelle kode filtrerer både før beregning og efter beregning: `buildMidlertidigtEetSourceResult(...)` beregner kun videre hvis der findes mindst én ikke-tom ASL-række med `Midlertidig` eller `Delvist endelig`, og `buildMidlertidigtEetAfgoerelseGroupsFromComputation(...)` skipper igen alt andet end de to typer. Det reducerer risikoen for at `Endelig` importeres, men kontrakten bør stadig beskrive dette som en invariant om *ingen importrelevante grupper fra Endelig*, ikke kun som en filterregel. Optimalt bør der være en test, der ændrer EET-input med både midlertidig, delvist endelig og endelig afgørelse og dokumenterer at EO-snapshot/PDF kun modtager de to første. Hvis en fremtidig EET-beregning returnerer en gruppe med ukendt afgørelsestype, bør EO-importen fejle lukket frem for at tolke ukendt som irrelevant.

---

### 4. Medium — `periodisering-contract.md` — Manglende dækning af Forsørgertab og EET

**Lokation:** `periodisering-contract.md`, generelt

**Problem:** Kontrakten begrænser sit scope til EO-periodisering (`§1`, `§3`, `§4`, `§5`) og nævner kun EO-domænet eksplicit. Forsørgertab og Erhvervsevnetab har egne periodiseringsregler (kapitalisering, løbende-ydelsesrækker, arbejdsdagstælling). Det er uklart om disse domæner er bundet af denne kontrakt, undtaget fra den, eller slet ikke dækket.

**Risiko:** Implementeringer i EET og Forsørgertab kan bruge lokale ad hoc-periodiseringslogikker uden at dette er et kontraktbrud — fordi kontrakten ikke gør krav på dem. Duplicerede periodiseringsregler i disse domæner vil ikke fanges af reviewers der håndhæver `periodisering-contract.md`.

**Anbefaling:** Tilføj et scope-afsnit der eksplicit angiver hvilke domæner kontrakten binder (`EO`, `EET`, `FST`, alle?), og om `periodiseringsMotor.ts` er kanonisk for alle domæner eller kun EO. Hvis andre domæner har egne motorerr, beskriv da forholdet.

**Worker-supplering:** Kodebasen viser, at problemet er bredere end EET og Forsørgertab. `src/utils/periodeBeregning.ts` indeholder årslønsperioder, hverdagsoptælling, normtal (`STANDARD_HVERDAGE_PAA_AAR`, `STANDARD_SH_DAGE_PAA_AAR`) og adaptere til EO-motorens offentlige-ydelelsesdage. `src/domain/aarsloen/aarsloenCalculations.ts` bruger disse årslønsregler direkte. Det er sandsynligvis domænemæssigt korrekt, at årslønsomregning ikke følger EO's TAF-periodiseringsmotor 1:1, men kontrakten siger i dag generelt, at kode der periodiserer beløb eller tæller periodiseringsdage uden at følge denne kontrakt er arkitektonisk fejl. Det gør eksisterende Årsløn enten uretmæssigt kontraktstridig eller utilsigtet udækket. Optimalt bør kontrakten indføre en periodiserings-taksonomi: EO-TAF/offentlige ydelser, Årsløn/omregning, EET-løbende ydelser og Forsørgertab/kapitalisering. For hver kategori bør den angive kanonisk motor, hvilke normtal der er tilladt, og hvilke begreber der ikke må genbruges på tværs.

---

### 5. Medium — `snapshot-contract.md` §3 — "Minimumsindhold" er ikke minimumsindhold

**Lokation:** `snapshot-contract.md` §3: *"Et domæne-snapshot skal som minimum returnere de projektioner, som dets forbrugere behøver..."*

**Problem:** Formuleringen cirkulerer: minimumsindholdet er defineret som "det forbrugerne har brug for" — men det er præcis det der skal specificeres. Listens fire punkter (`input-afledte visningsprojektioner`, `blocking-/issue-information`, `PDF-projektion`, `direkte reference`) er generiske nok til at enhver implementation kan hævde compliance.

**Risiko:** Nye domæner kan løftes til snapshot-first med snapshots der mangler centrale projektioner, fx blocking-information til tab-laget, fordi "minimumsindholdet" ikke er operationelt afgrænsende. Kontrakten garanterer ikke at parallel beregningslogik faktisk fjernes.

**Anbefaling:** Omskriv §3 til et checklist-format: for hvert af de to snapshot-former (§4.1 og §4.2), specificér konkret hvilke nøgler der *skal* være til stede. Punkt-4 ("direkte reference til den autoritative domæneberegning") er et implementeringsdetalie der ikke hører hjemme i et minimumsindholdsafsnit — fjern det eller flyt det til §4.

**Worker-supplering:** De tre faktiske snapshot-shapes er meget forskellige: `computeForsoergertabSnapshot(...)` returnerer `fieldUi`, gates, `calculation` og `pdfProjection`; `computeEetSnapshot(...)` returnerer tabprojektioner med `issues`, `hasBlockingErrors` og `computation`; `computeEoSnapshot(...)` returnerer status, invariants, canonical output, debug snapshot, PDF-model og original committed input. Det understøtter kontraktens valg om ikke at kræve én fælles teknisk type, men det gør minimumsindholdskravet endnu vigtigere. En bedre kontrakt bør kræve, at hvert snapshot-entrypoint eksplicit deklarerer: autoritativ inputpakke, projections der må forbruges af UI/PDF/debug, gating-felter, fejlklassifikation, runtime-fail-closed adfærd og om `input` er original committed state eller effektiv/transient state. EO er især vigtig her, fordi `snapshot.input.erstatningsopgoerelse` bevarer original committed state mens beregningerne bruger effektiv state med transient EET-injection.

---

### 6. Medium — `eo-snapshot-contract.md` §4 — Tabel: `error` vs. `fail_closed` skellet er ikke analogt

**Lokation:** `eo-snapshot-contract.md` §4, statustabel

**Problem:** Tabellen siger at `fail_closed` dækker "Schema-guard fejler, runtime-undtagelse i snapshot-build, *eller* `blocksAuthoritativeComputation`-invariant er brudt" — men §3.1 siger at `blocksAuthoritativeComputation: true` stopper engines og sætter snapshot til `status: 'error'`. De to afsnit er modstridende: §3.1 siger `error`, §4-tabellen siger `fail_closed` for den samme tilstand.

**Risiko:** Implementeringer der matcher på `fail_closed` og `error` separat vil behandle `blocksAuthoritativeComputation`-brud inkonsistent. Projektioner der forventer `data: null` ved `fail_closed` men `data` tilgængeligt ved `error` vil gå ned eller vise forkert tilstand.

**Anbefaling:** Harmonisér §3.1 og §4-tabellen. Beslut om `blocksAuthoritativeComputation` → `fail_closed` (§4) eller `error` (§3.1) — ét svar, ét sted. Fjern den modsatrettede tekst i den anden.

**Worker-supplering:** Den aktuelle implementering i `src/domain/erstatningsopgoerelse/snapshot/eoSnapshot.ts` følger i praksis §3.1 for validator-/invariantblokeringer: `hasAuthoritativeBlockingInvariant(validationInvariants)` returnerer `status: 'error'`, `data: null` og et delvist `debugSnapshot`. Schema-guard, skjult intern løn-state og runtime exceptions returnerer derimod `status: 'fail_closed'` med `failClosedReason`. Tabellen i §4 er derfor ikke bare intern kontraktmodstrid; den er også ude af sync med koden. Den bedste slutmodel er efter min vurdering et treleddet skel: `fail_closed` = system-/schema-/runtime-tilstand hvor autoritativ beregning ikke må bruges; `error` med `data: null` = forventelig bruger-/validatorblokering hvor debug kan vise sikker delprojektion; `error` med `data` = output-specifik blokering efter beregning. Hvis dette skel fastholdes, skal §4-tabellen udvides med `data`-/`debugSnapshot`-garantier pr. status, ellers vil projektioner fortsætte med at gætte på statussemantik.

---

### 7. Medium — `eo-snapshot-contract.md` §13 — EET-issues overføres "ukritisk" — men kontrakten accepterer det bevidst

**Lokation:** `eo-snapshot-contract.md` §13: *"EET-issues overføres bevidst ukritisk og uden EO-specifik relevansfiltrering."*

**Problem:** Kontrakten dokumenterer et bevidst designvalg med et eksplicit trade-off (manglende EET-beregningsdato kan blokere EO-output, selv om EO-importen ikke bruger den dato). Valget er begrundet og accept er eksplicit. Det er dog en løbende vedligeholdelsesbyrde: hver ny EET-issue-type skal vurderes manuelt om den må propagere til EO — og kontrakten indeholder ingen mekanisme til at markere undtagelser.

**Risiko:** Lav risiko for fejl nu, men høj risiko for fremtidig kontraktdrift. Nye EET-issues tilføjes uden at nogen revurderer om de bør nå EO. Over tid kan det lede til EO-blokering på grunde brugeren ikke kan forstå.

**Anbefaling:** Tilføj en note om at ændringer i EET-issue-typer skal revurdere §13-implikationen og opdatere eo-snapshot-kontrakten. Det gør trade-offet aktivt frem for implicit accepteret-og-glemt.

**Worker-supplering:** `buildMidlertidigtEetSourceResult(...)` returnerer EET-issues sammen med grupperne, men returnerer `{ groups: [], issues: [] }` hvis importkilden mangler, eller hvis der ikke findes importrelevante ASL-rækker. Det er fint for "ingen relevante rækker", men svagere for "kilden kunne ikke bygges sikkert". `useMidlertidigtEetInsertSource(...)` konstruerer i dag en kilde ved at safeParse sektioner og falde tilbage til initial values/`undefined` ved parsefejl. Det er robust mod crashes, men det betyder at en invalideret eller ufuldstændig EET-/stamdata-sektion kan blive omsat til tom import frem for en tydelig EO-issue. Under den nuværende invariant om schema-valideret committed state bør det ikke ske i normal drift; kontrakten bør alligevel sige, at "manglende kilde" og "kilde findes men er ikke schema-valid" er forskellige tilstande. Optimalt bør førstnævnte kunne være funktionelt tom, mens sidstnævnte skal være en system-/preflight-issue der ikke maskeres som ingen EET-data.

---

### 8. Medium — `domain-boundary-contract.md` §5 — Sidegrænser-listen og sektionslisten (§1.2) er ikke i sync

**Lokation:** `domain-boundary-contract.md` §1.2 og §5

**Problem:** §1.2 lister sektionerne `aarsloen`, `renteberegning`, `varigemen`, `forsoergertab`, `erstatningsopgoerelse`, `erhvervsevnetab`. §5 lister siderne `Erstatningsopgørelse`, `Erhvervsevnetab`, `Varige mén`, `Årslønsberegning`, `Renteberegning`, `Satser`, `Forsørgertab`. `Satser` nævnes i §5 som en side med sidegrænser, men har ingen tilsvarende post i §1.2's sektionsliste. Tilsvarende mangler `aarsloen` en korresponderende sidepost i §5 (`Årslønsberegning` er nævnt, men sektionen hedder `aarsloen`).

**Risiko:** Lav — men inkonsekvensen kan forvirre ved kontraktændringer: er `satser` en domæne-sektion eller kun en sags-global? §1.1 lister `satser` som sags-global, hvilket er rigtigt — men så burde `Satser`-siden ikke figurere i §5 som side med domænegrænser mod andre sider (sektionen tilhører alle).

**Anbefaling:** Ryd op: fjern `Satser` fra §5-listen (det er en sags-global sektion, ikke en domæne-sektion med sidegrænser), eller tilføj en note om at §5 dækker både fagsider *og* sider der skriver sags-globale sektioner.

**Worker-supplering:** Quality-testen `domainBoundaryIsolation.test.ts` viser, at den operationelle model allerede behandler `Satser` som en side med adgang til `satser` og `stamdata`, selv om `satser` i kontrakten er sags-global. Det taler for at §5 ikke skal hedde "Følgende fagsider" i snæver forstand, men "persistensskrivende sider". Kontrakten bør skelne mellem domæne-sektionsejerskab og page-boundary-regler: en side kan have en sags-global skrivefunktion uden at være et fagdomæne. Ellers opstår samme uklarhed hver gang en sags-global sektion har en egen side.

---

### 9. Lav — `periodisering-contract.md` §2 — `Hverdage`-definitionen er inkonsistent med brug

**Lokation:** `periodisering-contract.md` §2: *"`Hverdage`: mandag-fredag uden fradrag for SH-dage eller ferie."*

**Problem:** §5 om sygedagpenge siger at SH-dage *medregnes* i arbejdsdagsperiodisering til og med 2012-07-01. Dette indebærer at "arbejdsdage" for sygedagpenge pre-2012 inkluderer SH-dage — og er dermed identisk med det §2 kalder "hverdage". Den logiske konsekvens er at "hverdage" i §2 faktisk hedder "arbejdsdage" for pre-2012 sygedagpenge. Kontrakten kalder det ikke eksplicit ud.

**Risiko:** Terminologisk forvirring ved implementering af pre-2012 sygedagpenge-regler. Lav risiko i sig selv, men potentielt kilde til en fejl ved fremtidig ændring.

**Anbefaling:** Tilføj en note til §2 om at "hverdage"-definitionen er udgangspunktet, og at domænespecifikke undtagelser (som §5) kan gøre `arbejdsdage ≡ hverdage` for specifikke perioder.

**Worker-supplering:** Koden gør undtagelsen afhængig af rækkens slutdato (`rowTilISO < SYGEDAGPENGE_SH_CUTOFF`) i `isOffentligYdelseDatoMedregnet(...)` og `buildSygedagpengeArbejdsdagePrKalenderuge(...)`. Kontrakten formulerer derimod reglen som "Til og med 2012-07-01 medregnes SH-dage". Det er substantielt samme skæringsdato, men kontrakten bør være præcis om klassifikationsnøglen: er det hver enkelt dags dato, periodens slutdato eller en opsplittet periode ved cutover? Den aktuelle kode bruger periodens slutdato som samlet klassifikation. Hvis det er domænereglen, skal kontrakten sige det eksplicit; hvis ikke, er der en reel risiko for fejl i perioder der krydser 2012-07-02.

---

### 10. Lav — `snapshot-contract.md` — Manglende reference til `eo-snapshot-contract.md`

**Lokation:** `snapshot-contract.md` §6

**Problem:** §6 nævner `domain-boundary-contract.md`, `form-contract.md` og `page-component-contract.md` som relationelle kontrakter. `eo-snapshot-contract.md` er en direkte specialisering af snapshot-kontrakten, men er ikke nævnt — hverken som eksempel på en "domænespecifik kontrakt" (§6.4) eller som reference.

**Risiko:** En reviewer eller ny udvikler der læser snapshot-kontrakten som udgangspunkt, har ingen åbenlys sti til `eo-snapshot-contract.md`, som er langt mere specifik og styrende for den faktiske EO-implementering.

**Anbefaling:** Tilføj `eo-snapshot-contract.md` som eksempel i §6.4 på en domænespecifik kontrakt der indsnævrer snapshot-entrypoint-semantikken.

**Worker-supplering:** EO bør også nævnes i §4.2 som et aktuelt issue-/tab-/projektionssnapshot, ikke kun i relationelle kontrakter. Den faktiske EO-shape er mere kompleks end EET, men netop derfor er den et bedre eksempel på, at snapshot-formerne er mønstre og ikke faste typer. Hvis EO ikke nævnes, fremstår `computeEoSnapshot(...)` som et særtilfælde uden for den generelle snapshot-kontrakt, selv om `eo-snapshot-contract.md` er den mest modne specialisering.

---

### 11. Høj — `domain-boundary-contract.md` §9 og `eo-snapshot-contract.md` §13 — EO-specialhook går uden om de kanoniske persistence-adgangsniveauer

**Lokation:** `src/hooks/useMidlertidigtEetInsertSource.ts` sammenholdt med domain-boundary §9 og AGENTS-reglen om kanoniske hooks for persistence-adgang.

**Problem:** EO-specialhooket bruger `formPersistenceStore` direkte via `useSyncExternalStore`, ikke `usePersistedSectionSelector(pageKey)`. Koden er snæver og dækket af `domainBoundaryIsolation.test.ts`, men kontrakterne beskriver ikke, at denne type read-only direct-store hook er tilladt som systemnær undtagelse. Dermed findes der en implementeret arkitekturundtagelse uden kontraktmæssig placering.

**Risiko:** Fremtidige hooks kan kopiere mønsteret og læse direkte fra storen med henvisning til EO-specialhooket, uden at de får samme snævre testdækning eller read-only begrænsning. Det svækker persistence-adgangsniveauerne og gør skjulte tværdomæneafhængigheder lettere.

**Anbefaling:** Tilføj enten en eksplicit undtagelse til domain-boundary §9/§10 eller flyt specialhooket ind under persistence-infrastrukturens kontrakt som en navngiven, testdækket systemfacade. Kontrakten bør kræve, at enhver direct-store tværdomænelæsning har: navngiven domæneundtagelse, read-only garanti, quality-test der låser tilladte sektioner, og forbud mod at skrive eller injicere defaults i committed state.

---

### 12. Medium — `eo-snapshot-contract.md` §13 — Snapshot-revision for transient EET er stærkere i kode end i kontraktens betingelse

**Lokation:** `eo-snapshot-contract.md` §13 og `src/components/pages/Erstatningsopgoerelse.tsx`

**Problem:** Kontrakten siger, at snapshot-revisionen skal inkludere `erhvervsevnetab` og `faellesAarsloen`, når togglen er `'Ja'`. Koden inkluderer revisionerne deterministisk uanset toggle, med en kommentar om at cachen derved invalideres ved EET-ændringer.

**Risiko:** Lav i nuværende kode, men kontrakten beskriver en svagere regel end implementeringen. Hvis en fremtidig ændring optimerer tilbage til "kun ved Ja", kan snapshot-cachen få et kantproblem ved toggle-skift eller ved stale `midlertidigtEetInsertSource`.

**Anbefaling:** Opdater kontrakten til enten at kræve den nuværende stærkere model (EO-snapshot revision inkluderer altid EO, stamdata, EET og `faellesAarsloen` revisioner, men EET-data ignoreres semantisk når togglen er `'Nej'`) eller at dokumentere præcist hvorfor betinget revision er sikkert. Det mest robuste slutprodukt er den nuværende stærkere revision, fordi den er enklere at auditere og gør cache-invalidering uafhængig af toggle-rækkefølge.

---

### 13. Medium — `snapshot-contract.md` og `eo-snapshot-contract.md` — Runtimefejlmodeller er usymmetriske på tværs af snapshots

**Lokation:** `computeEoSnapshot(...)`, `computeEetSnapshot(...)`, `computeForsoergertabSnapshot(...)`

**Problem:** Alle tre snapshots fanger runtime exceptions, men med forskellige former: EO returnerer `status: 'fail_closed'` og `failClosedReason`, EET returnerer en tabprojektion med issue `runtime-exception`, og Forsørgertab returnerer en beregningsresultat-issue via `createRuntimeExceptionCalculation(...)`. Snapshot-kontrakten kræver side-effect-free/read-only, men beskriver ikke minimumskrav til runtimefejl på tværs af snapshot-former.

**Risiko:** UI/PDF-forbrugere kan få forskellig fail-closed-semantik afhængigt af domæne, uden at kontrakten fortæller hvad der er autoritativt. Det øger risikoen for at et domæne viser delresultater efter en intern beregningsfejl, mens et andet blokerer alt.

**Anbefaling:** Bevar gerne forskellige shapes, men tilføj et tværgående snapshot-krav: uventede runtimefejl må aldrig producere gyldige totals/PDF-projektioner; de skal rapporteres via `reportSystemIssue` efter error-debug-kontrakten; og snapshot-projektionen skal indeholde en brugerrettet dansk fejl, som blokerer autoritativt output. Det er nok som fælles kontrakt og undgår en fælles teknisk status-enum.

---

### 14. Medium — `periodisering-contract.md` — Offentlige ydelser cutover-regler mangler krydsperiode-semantik

**Lokation:** `periodisering-contract.md` §5 og `periodiseringsMotor.ts`

**Problem:** Sygedagpenge-reglen beskriver datoen `2012-07-01`/`2012-07-02`, men ikke hvordan en ydelsesperiode der krydser skæringsdatoen skal behandles. Koden klassificerer ud fra periodens slutdato: hvis `rowTilISO < SYGEDAGPENGE_SH_CUTOFF`, fratrækkes SH-dage ikke; ellers fratrækkes SH-dage i hele periodiseringsvinduet.

**Risiko:** En fremtidig udvikler kan læse kontrakten som en dagsopdelt regel og splitte perioder ved cutover, hvilket vil give andre beløb end den nuværende motor. Omvendt kan den aktuelle kode være for grov, hvis domænereglen faktisk skulle være dagsopdelt. Kontrakten er ikke præcis nok til at afgøre det.

**Anbefaling:** Beslut og skriv den normative semantik eksplicit: enten "klassificeres efter ydelsesrækkens slutdato" eller "perioder der krydser cutover splittes og behandles med hver sin SH-regel". Det bør dækkes af regressionstest, fordi det er en beregningskritisk kant.

---

### 15. Lav — `domain-boundary-contract.md` §6.4 — Stinavn for fælles årsløn matcher ikke kodebasen

**Lokation:** `domain-boundary-contract.md` §6.4 punkt 5

**Problem:** Kontrakten siger, at fælles regler for `faellesAarsloen` skal implementeres i `src/domain/faellesAarsloen/`, men den aktuelle kode bruger `src/domain/aslEalAarsloen/` til `faellesAarsloenInitialValues.ts`, og flere regler ligger i fælles hooks/valideringsflows.

**Risiko:** Lav, men kontrakten peger på en mappe der ikke findes. Det gør fremtidig helper-discovery svagere og kan føre til en ny parallel mappe med samme ansvar.

**Anbefaling:** Ret kontrakten til den faktiske kanoniske placering, eller flyt/standardiser mappenavnet hvis `faellesAarsloen` ønskes som det normative domænenavn. For et optimalt slutprodukt bør sektionen have én navngiven canonical location for initial values, rules og feltvalidering.

---

## Endelige beslutninger og implementeringsplan

Dette afsnit omsætter fundene til konkrete beslutninger. Beslutningerne er plan for senere kontrakt-, kode- og testarbejde; denne fil er fortsat kun et review-/planlægningsdokument.

### Fund 1 — snapshot-kontrakt som passiv reference

**Beslutning:** Anbefalingen implementeres. `domain-boundary-contract.md` skal ikke nøjes med at referere til `snapshot-contract.md`; snapshot-first domæner skal registreres eksplicit med entrypoint, autoritative inputsektioner, output-forbrugere og minimumsprojektioner.

**Afledte rettelser:** `snapshot-contract.md` skal kræve en domænespecifik snapshot-deklaration for hvert snapshot-first domæne. Existing quality-test kan fortsætte med page-boundary-isolation, men der skal tilføjes test/review-gate for snapshot-entrypoints: ingen side effects, committed-only input og ingen parallel beregningslogik i UI/PDF.

**Implementeringsplan:** Først kontrakttekst i `domain-boundary-contract.md` og `snapshot-contract.md`. Derefter en lille statisk quality-test eller contract coverage-matrix, der lister `computeEoSnapshot`, `computeEetSnapshot` og `computeForsoergertabSnapshot`.

### Fund 2 — skriveejerskab for `faellesAarsloen`

**Beslutning:** Anbefalingen implementeres med neutral delt redigering, ikke feltvis eksklusivt ejerskab. Slutproduktet skal fortsat lade brugeren redigere fælles årsløn fra både EET og Forsørgertab, men kun via eksplicit brugercommit. Ingen page-local defaults, effects eller rehydrering må skrive `faellesAarsloen` automatisk.

**Afledte rettelser:** `domain-boundary-contract.md` skal definere `faellesAarsloen` som en delt sagssektion med multi-writer-undtagelse. Den skal kræve ens schema, initialValues og valideringsregler via `src/domain/aslEalAarsloen/`. Hvis en eksisterende side materialiserer defaults indirekte, skal den ændres senere.

**Implementeringsplan:** Opdater kontrakt og canonical path først. Tilføj derefter tests der viser, at navigation mellem EET og Forsørgertab ikke overskriver eksisterende committed `aslAarsloen`/`ealAarsloen` uden brugerinput.

### Fund 3 — EET-importfilter i EO

**Beslutning:** Anbefalingen implementeres. EO-import af midlertidigt EET skal fail-closed ved ukendt eller kontraktstridig afgørelsestype i importrelevant output. `Endelig` skal ikke importeres og skal dækkes af test; ukendt type skal ikke silently droppes som irrelevant.

---

## Opfølgende implementeringsreview (2026-05-24 — 3. runde)

### Lukkede fund siden forrige runde

**`parsedFaellesAarsloen`/`parsedStamdata` issues — IMPLEMENTERET:** Alle tre sektioner (EET, Forsørgertab, EO) emitterer nu `EetIssue` ved parse-fejl i `parsedFaellesAarsloen` og `parsedStamdata`. Fejlstier er dækket og brugeren får konkret feedback frem for silent no-op.

**2012-07-02 cutover regression — IMPLEMENTERET:** Regressionstest tilføjet i `periodiseringsMotor.test.ts` (linje 124–145) der verificerer korrekt overgang ved cutover-datoen. Fremtidige refaktoreringer kan ikke bryde denne grænse uden at testen fejler.

**Snapshot exception blocking (EET og FST) — IMPLEMENTERET:** Begge snapshot-funktioner (`computeEetSnapshot`, `computeForsoergertabSnapshot`) har nu tests der injicerer runtime-fejl og verificerer at snapshot-beregningen blokeres korrekt frem for at producere et delvist resultat.

### Stadig åbne fund

**`faellesAarsloen` write-ownership test — IKKE IMPLEMENTERET:** Ingen test verificerer at navigation mellem EET og Forsørgertab ikke overskriver en allerede committed årsløn uden brugerinput. Risikoen er reel: en fremtidig ændring i rehydrerings- eller default-logik kan indføre uønsket overskrivning uden at nogen test fejler. Der eksisterer ingen testsuite for dette scenarie.

**`useMidlertidigtEetInsertSource` hook-enhedstest — IKKE IMPLEMENTERET:** Downstream-kæden (insertion og row-transformation) er testet via integrationsstier, men `useMidlertidigtEetInsertSource` er ikke isoleret testet for schema-invalid-stien. Der eksisterer ingen `useMidlertidigtEetInsertSource.test.ts`. Fejl i schema-parsing i hooken er dermed kun synlige via end-to-end-adfærd, ikke ved unit-grænsen.

### Nye fund fra denne runde

Ingen nye fund.

---

**Dato:** 2026-05-24
**Filer gennemgået:** Baseret på fund fra foregående runder — ingen nye filer læst.

**Afledte rettelser:** `domain-boundary-contract.md` §9 og `eo-snapshot-contract.md` §13 skal definere invariant: importrelevante grupper må kun være `Midlertidig` eller `Delvist endelig`. EET/EO-tests skal dække blandede typer og ukendt type.

**Implementeringsplan:** Kontrakt først, derefter regressionstest for importkilden og EO-snapshot/PDF-projektion. Kodeændring kun hvis testen viser silent accept af ukendt type.

### Fund 4 — periodiseringsscope

**Beslutning:** Anbefalingen implementeres ved at udvide kontrakten til en periodiserings-taksonomi frem for at tvinge alle domæner ind i EO-motoren. EO-TAF, Årsløn/omregning, EET-løbende ydelser og Forsørgertab/kapitalisering skal hver have en kanonisk motor/helperplacering.

**Afledte rettelser:** `periodisering-contract.md` skal omskrives fra implicit EO-kontrakt til tværgående kontrakt med domæneafsnit. `periodiseringsMotor.ts` forbliver kanonisk for EO-TAF/offentlige ydelser, mens `periodeBeregning.ts`, årslønshelpers, EET- og Forsørgertab-motorer beskrives som separate kanoniske kilder.

**Implementeringsplan:** Kontraktarbejde først. Derefter audit af dagtællings-/periodiseringshelpers og tests pr. kategori. Ingen beregningslogik ændres, før kontrakten har fastlagt hvilke regler der faktisk skal være fælles.

### Fund 5 — snapshot minimumsindhold

**Beslutning:** Anbefalingen implementeres. `snapshot-contract.md` skal have checklist-baserede minimumskrav pr. snapshot-form, men stadig ikke kræve én fælles TypeScript-shape.

**Afledte rettelser:** Hvert snapshot-entrypoint skal deklarere autoritativ inputpakke, projections, gating/status, issue-model, PDF/debug-forbrug og runtimefejl-semantik. EO skal også deklarere original committed input vs. effektiv transient input.

**Implementeringsplan:** Kontrakten opdateres med checklist. Derefter tilføjes en coverage-test eller snapshot-entrypoint testmatrix, der dokumenterer at EO/EET/FST opfylder listen.

### Fund 6 — EO status `error` vs. `fail_closed`

**Beslutning:** Anbefalingen implementeres ved at fastholde den nuværende kodebaserede treledede model. `fail_closed` reserveres til system-/schema-/runtimefejl. `error` med `data: null` dækker forventelige bruger-/validatorblokeringer før autoritativ beregning. `error` med data dækker output-specifik blokering efter beregning.

**Afledte rettelser:** `eo-snapshot-contract.md` §3.1 og §4 skal harmoniseres. Tabellen skal beskrive `data`, `debugSnapshot`, `failClosedReason` og brugerrettet output pr. status. UI/PDF må ikke gætte på status ud fra kun statusnavn.

**Implementeringsplan:** Kontrakt først, derefter tests for validatorblokering, schema-guard/runtime exception og post-computation output issue.

### Fund 7 — EET-issues i EO

**Beslutning:** Anbefalingen implementeres med bevidst ukritisk propagation bevaret. Hver ny EET-issue-type skal dog revurdere EO-konsekvensen. Der skal skelnes mellem "ingen relevante EET-rækker" og "EET-kilden kunne ikke bygges schema-sikkert".

**Afledte rettelser:** `eo-snapshot-contract.md` §13 skal tilføje en ændringsregel for EET-issues og kildevaliditet. `useMidlertidigtEetInsertSource`-relaterede tests bør dække invalid kilde som særskilt tilstand, hvis den kan opstå udenfor kontraktbrud.

**Implementeringsplan:** Kontrakt først. Derefter test/audit af EET-issue-typer og EO-propagation.

### Fund 8 — sidegrænseliste og sektionsliste

**Beslutning:** Anbefalingen implementeres ved at omdøbe §5's model til persistensskrivende sider. `Satser` skal blive i listen, men med klar markering som sags-global side, ikke fagdomæne.

**Afledte rettelser:** `domain-boundary-contract.md` skal skelne mellem domæne-sektionsejerskab, sags-globale sektioner og side-boundary-regler. Quality-testens liste skal matche den terminologi.

**Implementeringsplan:** Kontrakttekst først, derefter eventuel testnavngivning/kommentaropdatering uden ændring af adgangsregler.

### Fund 9 — hverdage/arbejdsdage og SH

**Beslutning:** Anbefalingen implementeres. Kontrakten skal sige, at "hverdage" er basisbegreb, og at domænespecifikke regler kan gøre arbejdsdage lig hverdage for konkrete perioder.

**Afledte rettelser:** `periodisering-contract.md` skal samtidig vælge cutover-semantik for sygedagpenge. Den nuværende kodebaserede regel, klassifikation efter ydelsesrækkens slutdato, skal være normativ medmindre domænekrav senere siger andet.

**Implementeringsplan:** Kontrakt og regressionstest for perioder før, efter og på tværs af 2012-07-02.

### Fund 10 — reference til EO snapshot

**Beslutning:** Anbefalingen implementeres. `snapshot-contract.md` skal nævne `eo-snapshot-contract.md` som domænespecifik specialisering og `computeEoSnapshot(...)` som et aktuelt issue-/tab-/projektionssnapshot.

**Afledte rettelser:** Ingen kodeændring nødvendig. Contract coverage-matrix kan senere sikre, at domænespecifikke snapshot-kontrakter er krydsrefereret.

**Implementeringsplan:** Kontraktopdatering sammen med Fund 5.

### Fund 11 — EO-specialhook direkte store-adgang

**Beslutning:** Anbefalingen implementeres som navngiven undtagelse. `useMidlertidigtEetInsertSource` må fortsat bruge direkte read-only store-adgang, men kun som kontraktligt registreret systemfacade med testdækkede sektioner og forbud mod writes/default-injektion.

**Afledte rettelser:** `domain-boundary-contract.md` og persistence-adgangsreglerne skal nævne undtagelsen eksplicit. Quality-testen skal låse tilladte sektioner og importforbud mod skrive-API'er.

**Implementeringsplan:** Kontrakt først, derefter teststramning hvis nuværende `domainBoundaryIsolation.test.ts` ikke dækker alle krav.

### Fund 12 — snapshot-revision for transient EET

**Beslutning:** Anbefalingen implementeres ved at gøre den stærkere nuværende model normativ. EO snapshot-revision skal altid inkludere EO, stamdata, EET og `faellesAarsloen` revisioner; toggle `'Nej'` påvirker kun semantik, ikke cache-invalidering.

**Afledte rettelser:** `eo-snapshot-contract.md` §13 skal ændres fra betinget til deterministisk revision. Tests skal sikre cache-refresh ved EET-ændring også når togglen skifter efterfølgende.

**Implementeringsplan:** Kontrakt først. Koden er sandsynligvis allerede compliant; tilføj/justér test hvis dækning mangler.

### Fund 13 — runtimefejlmodeller på snapshots

**Beslutning:** Anbefalingen implementeres som tværgående invariant, ikke fælles status-enum. Uventede runtimefejl må aldrig give gyldige totals/PDF-projektioner, skal rapporteres som system issue efter error-debug-kontrakten, og skal give dansk blokerende brugerbesked.

**Afledte rettelser:** `snapshot-contract.md` skal have et fælles runtimefejl-afsnit. EO/EET/FST-kontrakter eller tests skal dokumentere deres domænespecifikke shape.

**Implementeringsplan:** Kontrakt først, derefter tests der simulerer exception i hvert snapshot-entrypoint og kontrollerer blokering.

### Fund 14 — offentlige ydelser cutover

**Beslutning:** Anbefalingen implementeres med nuværende kode som norm: sygedagpenge-SH-reglen klassificeres efter ydelsesrækkens slutdato, ikke ved automatisk dagsopdeling af krydsperioder.

**Afledte rettelser:** `periodisering-contract.md` skal beskrive slutdato-baseret klassifikation og konsekvensen for perioder, der krydser 2012-07-02. Hvis domænereglen senere ønskes dagsopdelt, er det en brugerobserverbar beregningsændring og kræver særskilt beslutning.

**Implementeringsplan:** Kontrakt og regressionstest for krydsperiode.

### Fund 15 — canonical mappe for fælles årsløn

**Beslutning:** Anbefalingen implementeres ved at rette kontrakten til eksisterende canonical placering `src/domain/aslEalAarsloen/`. Der oprettes ikke ny `src/domain/faellesAarsloen/`-mappe nu, fordi det ville være ren navneoprydning med unødvendig churn.

**Afledte rettelser:** `domain-boundary-contract.md` skal opdatere sti og placere initial values, validators og fælles rules i samme canonical område. Eventuelle spredte helpers kan senere flyttes, hvis det reducerer faktisk duplikation.

**Implementeringsplan:** Kontrakt først. Senere audit for helpers, men ingen mappeflytning som del af denne beslutning.

## Arkitektoniske grundprincipper

### `domain-boundary-contract.md`

Fundamentet — sags-global vs. domæne-sektion, eksplicit skriveejerskab, fail-closed ved tvivl — er solidt og korrekt valgt for et system med komplekse tværdomæne-afhængigheder. Reglen om at governance-problemer løses med eksplicit skriveejerskab og ikke via mikro-sektioner (§2, rationale) er et godt og håndhæveligt princip.

Det svageste punkt er §6.4's symmetriske skriveadgang til `faellesAarsloen`. Det er den eneste sektion i systemet hvor to domæner har fuld skriveadgang til de *samme* felter uden at kontrakten specificerer hvad der sker ved concurrent writes. I et desktop-applikation uden real-time synkronisering er risikoen lav men reel ved navigationsrækkefølge. Den renere løsning ville være at fordele felterne: `aslAarsloen` ejes af ét domæne, `ealAarsloen` af det andet — og kontrakten beskriver det.

EO-undtagelsen (§9) er den rette tilgang til krydsdomæne-integrationsbehov: eksplicit, snævert afgrænset, med klare regler for hvad der er tilladt. Alternativet — at lade EET og EO dele en sektion — ville have skabt et mere komplekst ejerskabsproblem. Den valgte løsning er den rigtige.

Worker-supplering: Den faktiske quality-test viser, at domain-boundary-kontrakten allerede er på vej til at være maskinhåndhævet. Det bør løftes ind i kontrakten som en norm: page-boundary-listen og quality-testen skal udvikles sammen. Det er særligt vigtigt fordi testens regex er bevidst smal og ikke fanger aliasing, destrukturering eller alle former for store-adgang. Kontrakten bør derfor ikke give indtryk af fuld statisk sikkerhed; den bør sige, hvad testen håndhæver, og hvad der stadig kræver manuel review.

### `periodisering-contract.md`

Kontraktens fundament er rigtigt: en kanonisk motor, forbud mod lokale ad hoc-beregninger, og krav om at kilde-motorens opdatering går forud for kontraktopdateringen (§8). Disse tre regler eliminerer den klassiske drift-risiko i beregningstunge systemer.

Det arkitektonisk svage punkt er scope-uklarhed (fund #4). Periodisering er et tværdomæne-problem, men kontrakten behandler det som EO-specifikt. Resultatet er at EET og Forsørgertabs periodiseringslogik er ureguleret fra denne kontrakt. Hvis den kanoniske motor i `periodiseringsMotor.ts` faktisk er tværdomæne, skal kontrakten sige det. Hvis EET og Forsørgertab har egne motorer, skal kontrakten forklare forholdet. Den nuværende tavshed er et hul, ikke en undtagelse.

Worker-supplering: Et optimalt slutprodukt bør ikke nødvendigvis tvinge alle domæner ind i EO's `periodiseringsMotor.ts`. Årsløn har legitime normtal og omregningsregler, og EET/Forsørgertab har kapitaliserings- og løbende-ydelseslogik med egne domænebegreber. Det vigtige er at kontrakten ophører med at være halvt generel og halvt EO-specifik. Den bør enten omdøbes til EO-periodisering eller udvides til en central "dato-, dagtællings- og periodiseringskontrakt" med domæneafsnit og kanoniske helperplaceringer.

### `snapshot-contract.md`

Snapshot-kontrakten er på det rigtige abstraktionsniveau: den specificerer hvad der er et snapshot (committed-only, read-only, kanonisk entry) og de to tilladte former, uden at påtvinge en universel datastruktur (§7). Det er et godt designvalg der tillader domænespecifikke variationer.

Svaghederne er primært i §3 (fund #5): minimumsindhold er cirkulært defineret og ikke operationelt afgrænsende. Kontrakten burde specificere minimumsindhold pr. form (§4.1 og §4.2) med konkrete krav, ikke generiske beskrivelser.

Et strukturelt spørgsmål er om snapshot-kontrakten bør kræve at domæner, der løftes til snapshot-first, dokumenterer dette i domain-boundary-kontrakten — ikke kun i deres egen domænekontrakt. I dag er EET og Forsørgertab snapshottede domæner, men det er kun implicit. At gøre det eksplicit i domain-boundary-kontrakten ville gøre systemets snapshot-topologi synlig ét sted.

Worker-supplering: Snapshot-kontrakten bør også definere forholdet mellem "original committed input" og "effektiv beregningsinput". EO bruger begge dele på grund af transient EET-injection. Det er et godt mønster, fordi save/load og UI stadig ser committed input, mens beregningen kan arbejde med et auditabelt effektivt input. Men hvis mønsteret ikke navngives, kan fremtidige domæner begynde at mutere eller persistere effektive værdier. Kontrakten bør derfor kræve, at snapshots med transient/virtuel input altid eksponerer original input uændret og holder den effektive inputsti internt eller eksplicit markeret.

### `eo-snapshot-contract.md`

EO-snapshot-kontrakten er den mest udbyggede og præcise af de fire. Clampingmodellen (§2) med dens to-delte skel (stille vs. fejlgivende clamping) og den fuldstændige behandlingsrækkefølge (§2.3) er eksemplarisk: det efterlader ingen tvetydighed om rækkefølge og semantik. Invariant-klassificeringen (§3) er klar og verificerbar.

Den vigtigste arkitektoniske svaghed er modstriden mellem §3.1 og §4 om hvad `blocksAuthoritativeComputation` resulterer i (fund #6). Det er ikke et abstraktionsniveauproblem — det er en intern inkonsistens i den samme kontrakt.

Et bredere arkitektonisk spørgsmål: EO er nu den eneste kontrakt der specificerer på dette detaljniveau (behandlingsrækkefølge trin 1–7, clampinggarantier, isometri-regler). Det er ikke i sig selv forkert — EO er systemets mest komplekse domæne — men det skaber en asymmetri: Forsørgertab og EET har domænekontrakter der kun beskriver entrypoint og sektionsadgang, mens EO har en kontrakt der beskriver selve beregningsmekanikken. Spørgsmålet for fremtidige review-punkter er om EET-beregningsmotoren ville have gavn af et tilsvarende detaljeret normativt dokument.

Worker-supplering: EO-kontrakten bør samtidig strammes, så den ikke bliver en blanding af normative regler, historiske migrationsnoter og implementeringskommentarer. De stærke afsnit er clamping, invariantklassificering, transient injection og output-konsistens. De svagere afsnit er dem, der blander statussemantik eller historik ind uden klar normativ effekt. En bedre sluttilstand er en kortere, skarpere EO-kontrakt plus eventuelle historiske noter i schema-evolution/persistence.

---

## Tilfældighedsfund

**T1 — `domain-boundary-contract.md`: Manglende version/historik**
Kontrakten har ingen version eller changelog-sektion. `eo-snapshot-contract.md` har `Version: 1.0`, de øvrige kontrakter har kun `Status: Gældende arkitektur`. For en normativ kontrakt der er den øverste autoritet i prioritetshierarkiet ville selv en minimal changelog (dato + ændring) reducere risikoen for at reviewers baserer sig på en forældet læsning.

**T2 — `periodisering-contract.md`: Prioritetslinje nævner `form-contract.md`**
Kontraktens header-linje: *"Underordnet `form-contract.md`, `domain-boundary-contract.md` og relevante domænekontrakter."* Det er den eneste kontrakt af de fire der angiver et prioritetshierarki eksplicit i headeren (ud over `eo-snapshot-contract.md`). `snapshot-contract.md` og `domain-boundary-contract.md` angiver ingen prioritet. Overvej om alle kontrakter bør have en konsistent prioritetslinje — eller om ingen af dem skal.

**T3 — `snapshot-contract.md` §4.1 og §4.2: Eksempeldomænerne er ikke symmetrisk navngivet**
§4.1 nævner `computeForsoergertabSnapshot(...)` og §4.2 nævner `computeEetSnapshot(...)`. EO-snapshot (`computeEoSnapshot`) er ikke nævnt som eksempel, selv om det er det mest komplekse og mest kontrakttunge snapshot i systemet. EO passer tættest til §4.2's form — et eksempel her ville gøre kontrakten mere informativ.

**T4 — `eo-snapshot-contract.md` §11: Bagudinkompatibilitets-note er placeret midt i en normativ kontrakt**
§11 indeholder en migrationshistorik om felt-omdøbning (`periodeTilBeregningFra` → `tafBeregningsperiodeFra`). Dette er historisk information, ikke en bindende arkitekturregel. Det passer bedre i `schema-evolution.md` eller som en note i `persistence-contract.md` end som en paragraf i en snapshot-kontrakt. Nuværende placering blander normativt indhold med historisk annotation.

**Bagudkompatibilitet:** §11's migrationsnote behøver ikke flyttes med det formål at bevare viden om feltnavnskift som en del af en fremtidig load-kompatibilitetssti. Historiske felt-omdøbninger i normative kontrakter kan fjernes uden erstatning; de er ikke en forpligtelse til at opretholde kompatibilitet med filer gemt under det gamle feltnavn. Fjern §11 fra `eo-snapshot-contract.md` — gem den ikke i `schema-evolution.md` som migrationshistorik der skal vedligeholdes.

**T5 — `domain-boundary-contract.md` §6.4: Ikke-eksisterende canonical mappe**
Kontrakten peger på `src/domain/faellesAarsloen/`, men den faktiske kode har `src/domain/aslEalAarsloen/`. Det er et lille, men konkret tegn på kontraktdrift og bør rettes før nye helpers placeres.

**T6 — `periodisering-contract.md`: "EO-periodisering" i §1 kolliderer med generel indledning**
Headeren siger, at dokumentet fastlægger periodisering, dagtælling og fradragsregler i Mineo, men §1 siger at den kanoniske kilde er for EO-periodisering. Enten skal headeren indsnævres, eller §1 skal udvides med domænekatalog.

---

## Sammenfatning

- Modstriden i `eo-snapshot-contract.md` mellem §3.1 og §4 om hvad `blocksAuthoritativeComputation` resulterer i (Høj, fund #6) er den eneste interne inkonsistens der kan skabe direkte implementeringsfejl — dette bør rettes.
- Symmetrisk skriveadgang til `faellesAarsloen` (Høj, fund #2) er det eneste sted i domain-boundary-kontrakten hvor concurrent writes ikke er reguleret. Det bør løses med feltbaseret ejerskab.
- `periodisering-contract.md`'s scope-uklarhed (Medium, fund #4) er et reelt hul: EET og Forsørgertab er ikke bundet af kontrakten, men anvender periodiseringslogik — kontrakt og virkelighed er ikke i sync.
- De fire kontrakter er generelt velfunderede og dækkende for EO-domænet, men mangler symmetrisk dybde for EET og Forsørgertab, hvis beregningsmekanik ikke er normativt fastlagt på kontraktniveau.
- `snapshot-contract.md`'s minimumsindhold (§3) er cirkulært og ikke verificerbart — det bør operationaliseres pr. snapshot-form.
- Worker-supplering: Den aktuelle kode har allerede gode byggesten til en stærkere kontraktmodel: page-boundary quality-test, single EO snapshot-entrypoint, EET/Forsørgertab snapshots og central EO-periodiseringsmotor. Hovedopgaven er derfor ikke at opfinde ny arkitektur, men at gøre kontrakterne præcise om eksisterende undtagelser, statussemantik, scope og håndhævelsesmekanismer.

---

## Opfølgende implementeringsreview (2026-05-24)

### Ændret status siden forrige review

**`useMidlertidigtEetInsertSource` schema-error masking — DELVIST IMPLEMENTERET**

Forrige status: Åben — schema-fejl maskeres som tom kilde, ingen brugerbesked.

Ny status: **Delvist implementeret.**

`useMidlertidigtEetInsertSource.ts` linje 41–47 emitterer nu et `EetIssue` med `severity: 'error'` og id `'midlertidigt-eet-source-schema-invalid'` når `parsedErhvervsevnetab.success === false`. Issuen propageres via `MidlertidigtEetInsertSource.issues` (linje 61). Kontraktkravet om at skelne "ingen relevante rækker" fra "kilde kunne ikke bygges schema-sikkert" er dermed opfyldt på type- og kildesniveau: en schema-invalid EET-sektion resulterer i en eksplicit `issues`-entry fremfor en stille fallback.

To delvist åbne punkter:

1. **Spread-fallback bevaret:** Linje 54–55 anvender stadig `...(parsedErhvervsevnetab.success ? parsedErhvervsevnetab.data : {})` som base i `eetValues`. En schema-invalid kilde giver fortsat `ERHVERVSEVNETAB_INITIAL_VALUES` som beregningsgrundlag — issues-mekanismen advarer, men forhindrer ikke beregning på initial values. `buildMidlertidigtEetSourceResult` i `midlertidigtEetInsertRows.ts` vil modtage issues og propagere dem som blocking EET-issues til EO-snapshot, hvilket i praksis bremser autoritativ beregning via `blocksAuthoritativeComputation`. Kæden er dermed funktionelt korrekt, men eetValues-konstruktionen er fortsat ikke eksplicit afskærmet fra beregning ved schema-fejl.

2. **Ingen test dækker schema-invalid-stien i `useMidlertidigtEetInsertSource`:** Der eksisterer ingen `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.ts`. Issuen emitteres og propageres korrekt i implementeringen, men ingen test verificerer at `getMidlertidigtEetInsertSourceSnapshot` returnerer `issues` med `severity: 'error'` ved schema-fejl, eller at dette resulterer i en blocking EO-snapshot.

**`contractCoverageMatrix.test.ts` — `eoSnapshot.test.ts` mangler i snapshot-contract.md-matrixen — IMPLEMENTERET**

Forrige status: Åben — `eoSnapshot.test.ts` manglede i `snapshot-contract.md`-entryen.

Ny status: **Implementeret.**

`src/__tests__/quality/contractCoverageMatrix.test.ts` linje 29–35 indeholder nu en dedikeret `snapshot-contract.md`-entry med `eoSnapshot.test.ts` som påkrævet testfil ved siden af `forsoergertabSnapshot.test.ts` og `eetSnapshot.test.ts`. Den asymmetri der blev påpeget i forrige review er lukket.

### Uændret åbne fund

**Medium — `faellesAarsloen` write-ownership test**

Stadig åben. Ingen test verificerer at navigation frem og tilbage mellem EET og Forsørgertab ikke overskriver en eksisterende committed `aslAarsloen`/`ealAarsloen` uden brugerinput. Der eksisterer ingen testsuite under `src/__tests__/` med dette fokus. Kontraktkravet i `domain-boundary-contract.md` §6.4 er dokumenteret, men ikke testdækket.

**Lav — 2012-07-02 cutover regression test**

Stadig åben. Grep over `src/__tests__/` finder ingen test der verificerer en sygedagpengeperiode der krydser `2012-07-02`. `periodiseringsMotor.test.ts` og `tafDaySets.test.ts` dækker motorens logik generelt, men intet test-case konstruerer specifikt en periode med slutdato efter cutover og en der krydser cutover, og sammenligner de resulterende arbejdsdage.

**Lav — snapshot exception blocking test (EET og FST)**

Stadig åben. `eoSnapshot.test.ts` linje 443–458 indeholder en test der kommenterer at `runtime_exception`-stien i `computeEoSnapshot` ikke kan trigges let udefra, og som i stedet verificerer strukturen via schema_guard-stien. Det er en dokumenteret placeholder, ikke en egentlig simuleret exception-test. `eetSnapshot.test.ts` og `forsoergertabSnapshot.test.ts` har ingen tests der simulerer en runtimefejl i snapshot-entrypointet og verificerer at beregning blokeres og `reportSystemIssue` kaldes.

### Nye fund fra denne runde

**Lav — `useMidlertidigtEetInsertSource.ts`: `parsedFaellesAarsloen`- og `parsedStamdata`-fejl emitterer ingen issues**

Lokation: `src/hooks/useMidlertidigtEetInsertSource.ts` linje 39–61.

Problem: `sourceIssues` emitteres kun hvis `parsedErhvervsevnetab.success === false`. Schema-fejl i `parsedFaellesAarsloen` (linje 39) og `parsedStamdata` (linje 40) giver ingen issue. En schema-invalid `faellesAarsloen`-sektion resulterer i silent fallback til `FAELLES_AARSLOEN_INITIAL_VALUES`; en schema-invalid `stamdata`-sektion giver `skadelidteFodselsdato: undefined` og `skadedato: undefined` uden rapportering. Kontrakten (`eo-snapshot-contract.md` §13) skelner "ingen relevante EET-rækker" fra "kilde kunne ikke bygges schema-sikkert", men dækker tilsyneladende kun `erhvervsevnetab`-sektionen i nuværende implementation, ikke de to øvrige sektioner der udgør kilden.

Risiko: En schema-invalid `stamdata`-sektion kan fx give `skadedato: undefined`, som `buildMidlertidigtEetSourceResult` bruger til at afgøre om import er relevant. Det kan give en forkert "ingen importrelevante rækker"-konklusion i stedet for "kilde invalid". Fejlen maskeres som manglende data fremfor ugyldig kilde.

Anbefaling: Udvid `sourceIssues`-logikken til at inkludere schema-fejl i `faellesAarsloen` og `stamdata` — eller dokumenter i kontrakten at kun `erhvervsevnetab`-schema-fejl anses som "kilde kan ikke bygges schema-sikkert", og de øvrige sektioner er acceptable at falde tilbage til defaults for.

---

**Dato:** 2026-05-24
**Filer gennemgået:**
- `src/hooks/useMidlertidigtEetInsertSource.ts`
- `src/__tests__/quality/contractCoverageMatrix.test.ts`
- `src/__tests__/domain/erstatningsopgoerelse/eoSnapshot.test.ts`
- `src/__tests__/domain/erstatningsopgoerelse/midlertidigtEetTransientInjection.test.ts`
- `src/__tests__/domain/erhvervsevnetab/eetSnapshot.test.ts` (grep)
- `src/__tests__/domain/forsoergertab/forsoergertabSnapshot.test.ts` (grep)
- `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetInsertRows.ts` (type-grep)

---

## Opfølgende implementeringsreview (2026-05-24 — 2. runde)

### Ændret status siden forrige review

**`parsedFaellesAarsloen`/`parsedStamdata` schema-fejl emitter nu issues — IMPLEMENTERET**

Forrige status (Lav — nyt fund fra første 2026-05-24-runde): Åben — schema-fejl i `faellesAarsloen` og `stamdata` gav silent fallback til initial values/`undefined` uden rapportering.

Ny status: **Implementeret.**

`useMidlertidigtEetInsertSource.ts` linje 50–63 emitterer nu dedikerede `EetIssue`-entries for begge sektioner: `'midlertidigt-eet-faelles-aarsloen-schema-invalid'` (linje 50–56) og `'midlertidigt-eet-stamdata-schema-invalid'` (linje 57–63). Alle tre safeParse-resultater propageres nu eksplicit som issues ved fejl. Det "Lav"-fund der blev rejst i forrige runde er lukket.

Konsekvens: Det Medium-fund om schema-error masking er nu markant tættere på lukket. Alle tre kildesektioner emitterer issues. Spread-fallback til initial values bevares stadig i `eetValues`-konstruktionen (linje 70–74), men blocking-kæden via EO-snapshot er funktionelt korrekt: issues med `severity: 'error'` propageres til `buildMidlertidigtEetSourceResult` og konverteres til `EoInvariant` med `blocksAuthoritativeComputation: true`. En schema-invalid kilde resulterer dermed i blokeret autoritativ beregning, ikke i stille fejlberegning på initial values.

**2012-07-02 cutover regressionstest — IMPLEMENTERET**

Forrige status (Lav): Åben — ingen test for sygedagpengeperiode der krydser skæringsdatoen.

Ny status: **Implementeret.**

`src/__tests__/domain/erstatningsopgoerelse/periodiseringsMotor.test.ts` linje 124–145 indeholder nu en eksplicit test: `'isOffentligYdelseDatoMedregnet bruger 2012-07-02 som reel sygedagpenge-cutover efter periodens slutdato'`. Testen verificerer at en dato med `rowTilISO: iso('2012-07-01')` tæller SH-dage med (`foerCutover === true`), og at samme dato med `rowTilISO: iso('2012-07-02')` ikke tæller SH-dage med (`paaCutover === false`). Grænsetilfældet er dermed testdækket.

Der er derudover en eksisterende test (`'countOffentligYdelsePeriodiseringsdage vurderer hele intervallet ud fra periodens slutdato ved cutoff'`) der verificerer semantikken med en syntetisk cutoff-dato. De to tests tilsammen dækker grænseadfærden tilstrækkeligt.

**Snapshot exception blocking — IMPLEMENTERET for EET og FST**

Forrige status (Lav): Åben — `eetSnapshot.test.ts` og `forsoergertabSnapshot.test.ts` manglede tests der simulerer en runtimefejl i snapshot-entrypointet.

Ny status: **Implementeret.**

`src/__tests__/domain/erhvervsevnetab/eetSnapshot.test.ts` linje 145–168 indeholder nu testen `'failer lukket med snapshot-issue hvis en EET-beregner kaster runtimefejl'`. Testen mocker `computeEetLoebendeYdelser` til at kaste, kalder `computeEetSnapshot`, og verificerer at `snapshot.loebendeYdelser.computation === null`, `hasBlockingErrors === true`, og at `issues` indeholder en entry med `id: 'runtime-exception'` og `severity: 'error'`.

`src/__tests__/domain/forsoergertab/forsoergertabSnapshot.test.ts` linje 208–233 indeholder tilsvarende testen `'failer lukket med snapshot-issue hvis forsørgertabsberegningen kaster runtimefejl'`. Testen mocker `computeForsoergertabCalculation` til at kaste og verificerer `canShowResult === false`, `canDownloadPdf === false`, og `issues` indeholder `'runtime-exception'`.

Alle tre snapshot-entrypoints (EO, EET, FST) har nu runtimefejl-tests. Funnet er lukket.

### Uændret åbne fund

**Medium — `faellesAarsloen` write-ownership test**

Stadig åben. Ingen test verificerer at navigation frem og tilbage mellem EET og Forsørgertab ikke overskriver en eksisterende committed `aslAarsloen`/`ealAarsloen` uden brugerinput. `src/__tests__/components/layout/MainLayout.navigationCommitGuard.test.tsx` dækker navigation/commit-guard-mekanismen generelt, men indeholder ingen test for `faellesAarsloen`-specifikt skriveejerskab. Kontraktkravet i `domain-boundary-contract.md` §6.4 er fortsat ikke testdækket på dette punkt.

**Medium — `useMidlertidigtEetInsertSource`: ingen hook-enhedstest for schema-invalid-sti**

Stadig åben. Der eksisterer ingen `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.ts`. `midlertidigtEetTransientInjection.test.ts` dækker nedstrømsadfærden (at et `issues`-array med `severity: 'error'` resulterer i blocking EO-snapshot), men ingen test verificerer at selve `getMidlertidigtEetInsertSourceSnapshot` returnerer `issues` ved schema-fejl i en eller flere af de tre sektioner. Den funktionelle kæde er korrekt, men hook-laget er ikke isoleret testet.

### Nye fund fra denne runde

Ingen nye arkitektur- eller korrekthedsfund.

---

**Dato:** 2026-05-24
**Filer gennemgået:**
- `src/hooks/useMidlertidigtEetInsertSource.ts`
- `src/__tests__/domain/erstatningsopgoerelse/periodiseringsMotor.test.ts`
- `src/__tests__/domain/erstatningsopgoerelse/midlertidigtEetTransientInjection.test.ts`
- `src/__tests__/domain/erhvervsevnetab/eetSnapshot.test.ts`
- `src/__tests__/domain/forsoergertab/forsoergertabSnapshot.test.ts`
- `src/__tests__/components/layout/MainLayout.navigationCommitGuard.test.tsx` (grep)
- `src/__tests__/hooks/` (glob — ingen `useMidlertidigtEetInsertSource`-test fundet)

---

## Opfølgende implementeringsreview (2026-05-24 — 4. runde)

### Status pr. fund

**`faellesAarsloen` write-ownership test — IMPLEMENTERET:** `src/__tests__/hooks/usePersistedForm.test.tsx` linje 325 indeholder nu testen `'overskriver ikke committed fælles årsløn med initialValues ved navigation/remount'`. Testen committer `aslAarsloen: { kind: 'number', value: 512000 }`, simulerer rerender med ny route (`/forsoergertab`), og verificerer at værdien stadig er `512000` efter remount. Kontraktkravet i `domain-boundary-contract.md` §6.4 punkt 6 er dermed testdækket. Funnet er lukket.

**`useMidlertidigtEetInsertSource` hook-enhedstest — IMPLEMENTERET:** `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.tsx` eksisterer og dækker præcis schema-invalid-stien. Testen sætter `faellesAarsloen`-sektionen til `{ aslAarsloen: { kind: 'number', value: -1 } }` (ugyldig ifht. schema) via `__setSectionUnsafe`, renderer hooken, og verificerer at `issues` indeholder `'midlertidigt-eet-faelles-aarsloen-schema-invalid'`. Hook-laget er dermed isoleret testet for den sti der tidligere manglede. Funnet er lukket.

### Nye fund fra denne runde

Ingen nye fund.

---

**Dato:** 2026-05-24
**Filer gennemgået:**
- `src/__tests__/hooks/usePersistedForm.test.tsx`
- `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.tsx`

---

## Opfølgende implementeringsreview (2026-05-24 — 5. runde)

### Statusoverblik

Alle fund fra 4. runde er bekræftet **IMPLEMENTERET**. Der er ingen åbne fund fra de foregående runder.

**`faellesAarsloen` write-ownership test:** Bekræftet lukket — `usePersistedForm.test.tsx` indeholder testen `'overskriver ikke committed fælles årsløn med initialValues ved navigation/remount'`.

**`useMidlertidigtEetInsertSource` hook-enhedstest:** Bekræftet lukket — `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.tsx` eksisterer og verificerer schema-invalid-issue fra hook-laget for `faellesAarsloen`-sektionen.

**Kildekode bekræftet:** `useMidlertidigtEetInsertSource.ts` emitterer korrekt issues for alle tre safeParse-fejl (EET linje 43–49, fællesAarsloen linje 50–56, stamdata linje 57–63). `midlertidigtEetTransientInjection.ts` wrapper `buildMidlertidigtEetAfgoerelseGroupsFromComputation` i try/catch og konverterer throws til EET-issues med `severity: 'error'`, som propagerer til EO-snapshot og udløser `blocksAuthoritativeComputation`.

### Nye fund fra denne runde

Ingen nye fund.

---

**Dato:** 2026-05-24
**Filer gennemgået:**
- `src/hooks/useMidlertidigtEetInsertSource.ts`
- `src/__tests__/hooks/useMidlertidigtEetInsertSource.test.tsx`
- `src/domain/erstatningsopgoerelse/helpers/midlertidigtEetTransientInjection.ts`
