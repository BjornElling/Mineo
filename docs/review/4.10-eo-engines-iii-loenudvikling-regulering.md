# Punkt: 4.10 EO-engines III: løn-/ydelsesudvikling og regulering

**Dato:** 2026-05-31
**Filer gennemgået:**
- `src/domain/erstatningsopgoerelse/engines/loenudviklingBeregning.ts` (1522 l.)
- `src/domain/erstatningsopgoerelse/engines/sygeferiegodtgoerelse.ts` (1525 l.)
- `src/domain/erstatningsopgoerelse/engines/indkomstSkadestidspunktBeregning.ts` (357 l.)
- `src/domain/erstatningsopgoerelse/engines/offentligeYdelserUdviklingBeregning.ts` (243 l. — overblik)
- `src/domain/erstatningsopgoerelse/engines/overenskomstReguleringShared.ts` (123 l.)
- `src/domain/erstatningsopgoerelse/engines/reguleringFormulaUtils.ts` (122 l.)
- `src/domain/erstatningsopgoerelse/engines/reguleringCoverage.ts`, `reguleringsBeregning.ts`, `reguleringsPresentation.ts`

**Tests kørt:** `npx vitest run src/__tests__/domain/erstatningsopgoerelse/ src/__tests__/domain/debug/` → 93 filer / 1518 passed. `npx tsc --noEmit` → 0 fejl.

## Fund og rettelser

1. **Medium (konvergens / duplikeret sandhed) — magisk literal `0.048` i stedet for den kanoniske `TAF_ARBEJDSDAG_TIL_MAANED_FAKTOR`.** Der findes en navngivet kanonisk konstant `TAF_ARBEJDSDAG_TIL_MAANED_FAKTOR = 0.048` (`helpers/tafBeregningsenhed.ts:44`), som den kanoniske motor bruger (`periodiseringsMotor.ts:347`). Men **to** sites brugte den rå literal `0.048` for samme fravær→måned-faktor:
   - `indkomstSkadestidspunktBeregning.ts:256` (i scope)
   - `eoDebugErstatningsopgoerelseModel.ts:2342` (4.14-scope, men duplikeret sandhed rettes hvor den findes, jf. AGENTS.md)
   - **Risiko:** Ændres faktoren ét sted, ville disse to drifte — og debug-/beregnings-divergens for samme semantik er eksplicit forbudt af periodisering-contract §7.
   - ✅ **Rettet** — begge bruger nu `TAF_ARBEJDSDAG_TIL_MAANED_FAKTOR`. Samme værdi, **ingen beregningsændring** — bekræftet af 1518 grønne tests + grøn typecheck.

## Tilfældighedsfund

1. **Verificerede falske alarmer (subagent-fund "Kritisk", afkræftet ved direkte verifikation):**
   - **"Division by zero på `maanederResult`/`arbejdsdage`" (`indkomstSkadestidspunktBeregning.ts:228, 299`).** Falsk: divisionen l. 228 er *inde i* `if (maanederResult && samletBeregningsgrundlagOre > 0)` (l. 227), og l. 299 er inde i `if (arbejdsdage > 0 && …)` (l. 293-297). Begge divisorer er fuldt guardede.
   - **"`beregnMaanederForDage` NaN via slice" (`indkomstSkadestidspunktBeregning.ts:247-250`).** Falsk: `periodeDage` bygges kun fra `dateToISO()`-validerede ISO-strenge; `month` er 1-indekseret og `Date.UTC(year, month, 0)` giver korrekt dage-i-måned (samme verificerede idiom som forsørgertab). `dageIMaaned` er altid 28-31.
   - **"sygeferie `segments[0]` uden guard" (`sygeferiegodtgoerelse.ts:879`).** Falsk: l. 872 returnerer ved `segments.length === 0` før l. 879.
2. **Verificerede bevidste/korrekte designvalg:**
   - **`allocateOreByWeights` (`sygeferiegodtgoerelse.ts:868`)** er én kanonisk fordelingsfunktion; zero-weight-fallbacken (hele beløb på første segment) er dokumenteret beslutningsnote (l. 875-878), og år-fordelingens `weight > 0 ? weight : dates.length` (l. 928) er en bevidst dag-tælle-fallback når der intet monetært grundlag er. Begge stier bruger *samme* funktion; restfordelings-loopet (l. 893-898) sikrer eksakt sum. Ikke en asymmetri-fejl.
   - **SFGG-referenceperiode delegerer korrekt SH-håndtering til `periodiseringsMotor.optaelArbejdsdageBreakdown`** — ingen lokal SH-genimplementering; overholder periodisering-contract §5 (central sygedagpenge-SH).
   - **`loenudvikling` regulerings-/indekseringskæde**: alle divisioner (base-index/pct) er guardet med `ensurePositiveFiniteNumber`/eksplicitte finite-checks; afrunding følger Mineos øre-konvention (summér præcist, afrund til sidst).
3. **Strukturel observation (ikke rettet) — `loenudviklingBeregning.ts` (1522 l.) og `sygeferiegodtgoerelse.ts` (1525 l.) er de to største engine-filer.** Begge har naturlige split-sømme (strategi-buildere hhv. allocation/referenceperiode/compute-lag). ⏭ **Ikke splittet nu:** det er et stort, risikofyldt refactor på to tunge, tested beregningsmotorer; filerne er trods størrelsen kohæsive (ét ansvar hver). Noteret som struktur-kandidat til et dedikeret, fokuseret split-greb frem for ad hoc midt i review.
4. **Mindre kode-kvalitet (ikke rettet):** udokumenteret epsilon `1e-12` i sygeferie 4-måneders-cap; let percent-parse-helper-overlap mellem `reguleringFormulaUtils` og `utils/numberParsing`. Lav værdi/risiko; noteret.

## Sammenfatning

- Løn-/ydelsesudviklings- og reguleringsmotorerne er korrekte, veldokumenterede og kontraktkonforme; SH-håndtering er centralt delegeret, divisioner guardede, afrunding øre-konsistent.
- Rettet: magisk `0.048`-literal erstattet med den kanoniske `TAF_ARBEJDSDAG_TIL_MAANED_FAKTOR` to steder (beregning + debug) — lukker en duplikeret-sandheds-/drift-risiko som periodisering-contract §7 forbyder. Behavior-equivalent.
- **Alle** subagenters "Kritisk"-beregningsfund var falske alarmer (guards findes upstream).
- To meget store filer noteret som split-kandidater til et senere dedikeret greb. Ingen beregningslogik ændret.
