[mcp_servers.playwright]
command = "npx"
args = ["--no-install", "playwright-mcp", "--headless", "--isolated", "--block-service-workers", "--caps=devtools", "--console-level=warning"]
startup_timeout_sec = 30
tool_timeout_sec = 600
